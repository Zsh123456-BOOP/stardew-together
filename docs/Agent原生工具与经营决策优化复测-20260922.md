# Agent 原生工具与经营决策优化复测

本轮按用户“开始，然后重新测试”落实五项优化，范围为独立新档正常时间一天；不改动旧测试窗口或个人存档，不部署到服务器。

## 实现与边界

- 原生 Tool Calls：AutoplayModel 通过 API tools 提交当轮工具，读取 message.tool_calls；点号映射为双下划线，执行器仍使用原业务名。整轮先校验调用数、已加载工具、唯一 ID、完整参数 JSON 与明确的参数类型，再执行。API 工具 schema 从 Catalog 的参数短写生成；有歧义的字段保留原契约并继续使用现有业务检查，未启用 DeepSeek Beta strict，不能宣称服务端保证所有参数正确。
- 只保留上一轮完整 assistant/tool 消息对，按 tool_call_id 关联。查询回执引用原始 result_id；异步动作只回传排队/接收状态，实际完成在后续原生回执确认。取消、查询依赖草案明确标记未执行；陈旧回复不落地，不产生已执行回执。完整 HTTP 请求体、回复体仍归档，不含认证头。
- 仓库：常驻 assets 明确列出原生共享箱、容量和建设目标状态。storage_expand 在现有可达仓库能够容纳待存物品时返回已具备能力、completed=0，不再建第二个目标；明确 additional:true 可追加。仓储政策复用已有在建仓库目标。真实容量缺口需备料时报告目标 ID 与尚未完成，而非创建后只返回笼统失败。
- 采购：排队的 player.buy 在未开商店且可从原生数据确定唯一目的地时，复用 player.procure 完成到店/开店/现场核价。必须已有明确数量、最高单价、预算、现金保留；不替模型补预算或报价，不改非金币交易，不删除显式 after 依赖。原样保留每个限额；实际消费仍走原生商店。
- 独立顺序：提示明确 sequence_after 用于顺序，after 只用于真实依赖。上一轮两条社交取消来自模型显式声明依赖购买，并非执行器能擅自删除的依赖。本轮不篡改这些显式业务关系。
- 清理：缺少活动订单的 work.run cleanup 在派原生动作前返回结构化阻碍和 farm.cleanup 入口；不擅自替模型授权清理范围。
- 劳动与现金：常驻 labor_budget 展示今日待办保守估算、剩余体力、下个晴天现有作物的手工浇水估算、自有未种种子、新地块基础锄地浇水成本。报价与布局也携带同一依据。扣除已观察洒水器覆盖，不假设次晨满体力、未知天气或未兑现收入；不新增隐性预留或种植上限。
- 上下文：去除无决策价值的空地坐标示例；当前任务进度已在 progression，不再每轮建议 progress.read；选种候选直接提供 farm.select_seeds，而非要求重新读取自身。相同查询与完整当前字段去重，合并同批相同回执，删除重复进度变化正文及配方说明。assets、labor_budget 受预算保护；原始证据仍可回读。
- 度量：原生工具 schema 与上一轮 tool_calls 都计入输入字符，避免把提示词移入 tools 后误报节省；API token 始终使用实际 usage，包含陈旧回复和重试。

## 验证

独立 AgentLab 预检目录：`work/native-tool-native-check`，Mods：`work/NativeToolCheckMods`，端口18828。真实模型 Tool Calls 回复可解析，6条原生查询送达；38项运行契约检查全通过。新增仓库幂等、采购预算和依赖保持检查通过。预检使用隔离夹具，不能算自主经营成绩。

离线检查557项通过，Python检查20项通过，完整Mod编译0警告、0错误。新一天自主测试结果在完成后补齐。原生API参考：[DeepSeek Tool Calls](https://api-docs.deepseek.com/zh-cn/guides/tool_calls/)、[Chat Completions API](https://api-docs.deepseek.com/api/create-chat-completion/)。

## 首轮复测暴露问题与修正

首轮运行 `69f3c92c6e3b45e0a5198e282f1b95f3`（代码ab85ada，存档449765644，`work/native-tool-one-day`）完成春1至春2正常过夜，17:20上床、次晨体力270。34株作物均已浇水，现金500→0，无已兑现收入。36次HTTP请求，原生Tool Calls语法均可解析；输入中位数11869，高于迁移前9967，不能宣称省token。一次未加载工具名被拒、一次不支持的social劳动、一次容量阻碍均如实记录；38项夹具通过不代表这些模型取舍正确。

按该轮真实请求定位并修正：

- 每工具重复的_plan/引用元数据约占3818字符，改为system统一声明、客户端校验；明确的业务参数直接转为对象/数组/整数/布尔/枚举和required，移除description里的重复参数短写，原语义由属性description/enum/required保留。嵌套plan任务也核验当轮已加载工具与参数，错误携带具体字段路径。
- 查询结果直接放入原生tool消息，pending_queries只留下对应ID，避免让模型收到工具调用的结果指针后再次查询。两处任一实际送达都纳入回执确认；仍不把queued当执行完成。
- 紧凑day、service_hours、goals常驻并受预算保护，防止“读过一轮后又只剩补读指针”的循环。原生任务中的tool引用参与选择，使社交任务直接带正确player.social入口。
- 第二次预检暴露远端箱子可达性判断使用地图默认入口，受新档布局影响。改为先解析当前位置的真实跨图路线，再从实际落点检查箱子相邻格；第三次独立预检 `work/native-tool-native-check-v3` 全部通过（真实API、38项运行契约）。失败预检完整保留，不计作自主经营。

修正版离线561项通过，Python20项通过；Mod编译0错误0警告。修正版一天会从独立新档开始，首轮过程中没有替换DLL或补资源。

修正版145e53e的第一份连续运行 `7f0def526c59489f85ea7933bf034561`（`work/native-tool-one-day-v2`）暴露参数提示歧义：模型两次调用不存在的_plan函数。到春1 13:00主动停止，0过夜；24次请求、输入中位数10179.5，仅为中断样本。已保存状态/请求/报告后关闭本任务该实例。恢复每工具精简的_plan字符串属性并明确它只是arguments字段，保留其他schema去重；不将其伪装为可执行工具或忽略未知调用。最终修正版另开新档完整复测。


第三轮068bb99（`work/native-tool-one-day-v3`，运行c832d4632a554654ae50e80bebe21a84，存档449767006）在春1 10:30因连续三次工具校验失败自动暂停，0过夜。18次请求、输入中位数10593；25株已浇水、现金300、体力162。这是失败样本，不计完整一天。前两次重新调用已经退出当轮工具集的选种工具，第三次读取了未开放的planting_execution分类。校验拒绝整批调用，未执行该批动作。

定位发现：被拒回复没有替换上一轮成功的原生消息对，错误只出现在decision_reasons，容易反复引用旧报价。修复后每个被拒call_id收到本批not_executed及具体错误；未知函数名和枚举允许值写入错误，重复ID的无效信封不回放，整批预校验及三次失败保护保持不变。context.read同步开放已经存在的种植状态字段。新增3项域检查通过（总564）；报告新增直接工具拒绝统计及单测，不再只统计异步任务失败而漏掉无可行种植等直接返回。相关Python报告5项检查通过。


## 最终修正版：完整一天及三阶段对照

运行代码 `ee000f4c358a98600f9198b6cdd237ff7b005f4e`；DLL SHA256 `69a698235147ddd6ebe504e11eabb4ca54a7c8f550731a56c740ee5db341d2e1`。独立新存档449767700，run `1daefea1fbee41a187d236807ebe3438`，目录 `work/native-tool-one-day-v4`，Mods `work/NativeToolDayV4Mods`，端口18840。模型deepseek-v4-flash，正常时间，测试中未换DLL、补资源或重启接续。不同阶段使用不同新档，随机布局、取舍和游戏阶段不同，属于观察对照，不是配对A/B。

**已完成正常一天，但输入成本优化没有达到目标。** 春1 19:10上床，春2 06:00原生保存，SleepDays=1；指定一天目标触发自动暂停，窗口保留。保存证据为本次epoch `1b2ca50090474353951f3076290fcd33/day-1.jsonl` 第2行，睡前账本为 `day-0.jsonl` 第2201行；报告completed_days=[0]。

- 领取15份免费种子，实际花300金购买15份防风草种子；30株作物睡前全部浇水，现金500→200，无出货回款。次晨体力270/270；次晨30株重新需要浇水是正常跨日状态。
- 已原生制作、放置1个箱子。18:10的assets实际记录容量36、占用0；目标agent-storage-0为fulfilled、Count=1、Completion=placed，无未完成的第二箱目标。空箱不能描述为已经存入一箱物资。
- 45次真实HTTP请求，44轮决策落地；45次均返回可解析的原生Tool Calls，无协议校验失败、未加载函数调用或全局停机。API结构正确不代表模型规划正确。
- 2条异步任务失败，均为social_access_unavailable。另有7条直接工具拒绝/阻碍记录：屋内布局1次、无可行种植3次、关闭的商店查询1次、向动作计划塞入查询1次、查询社交失败状态1次。最后一条重复呈现同一社交失败，不能把这两类直接相加当作独立故障数。旧完整一天按相同口径为5条异步任务错误、2条直接工具拒绝。
- 采样分摊的醒着时间790分钟：劳动107.38（13.6%）、交通241.89（30.6%）、阻塞342.80（43.4%）、等待模型97.93（12.4%）。旧完整一天劳动20.9%、阻塞25.9%；本轮没有证明经营效率更优。未覆盖收获日，不推断长期利润。

| 指标 | 最初未优化 | 第一轮压缩后 | 本轮原生Tool Calls修正版 |
| --- | ---: | ---: | ---: |
| 实际输入token中位数 | 12,733 | 9,967 | 11,122 |
| 输入字符中位数（包括工具） | 31,985 | 26,343 | 29,924 |
| 可见工具数 | 固定46 | 中位22 | 中位20 |
| 请求数 | 27，止于10:00 | 29，完整一天 | 45，完整一天 |
| 已观测输入token合计 | 348,384，非整天 | 284,755 | 510,640 |
| 输出token合计 | 8,419，非整天 | 8,754 | 6,927 |
| HTTP耗时中位数 | 2.431秒 | 2.324秒 | 1.222秒 |

和最初相比：单次输入token中位数降低12.7%，字符中位数降低6.4%，工具数量降低56.5%。最早讨论的34,469是10:00某一条请求的字符数，不能和11,122 token直接相除。按相同游戏时间截至10:00，本轮20次请求、230,599输入token，最初27次、348,384，已观测输入下降33.8%；这仍不是整天费用降幅，也不是相同存档配对实验。

和第一轮完整一天相比：本轮单次输入中位数增加11.6%，请求数增加55.2%，全天输入token增加79.3%。输出更短、单次HTTP更快，但HTTP总耗时74.78秒对73.46秒几乎相同；缓存比例46.7%对38.4%，输入token不能按同比直接换算账单。仍未达到常态6,000–8,000 token目标。

## 可复核输入、输出和后续判断

所有真实HTTP body及运行产物保存在被忽略的work目录，不进入Git，不含认证头。`work/native-tool-one-day-v4/`内：

- `manifest.json`、`log-index.json`：代码、DLL哈希、存档及原生日志定位。
- `latest-readonly-state.json`、`operating-report.json`：次晨暂停状态、日账本、失败与过夜证据。
- `model-context-report.json`、`comparison.json`：包含重试/中断样本、逐调用usage和各阶段对照。最初基线原始来源另见旧报告comparison.json中的baseline.source。
- `representative-median-call/`：11:20、call `8dcf610398f948599350985c9fa62b2c`，输入恰为11,122 token的代表调用，**不是最后一次调用**。组成：system 1,835字符、当前user状态14,922、API工具schema 12,720、上一轮原生消息441；合计29,918字符。
- `last-call/`：真正最后一次请求及模型回复（player__sleep）。每组目录有实际system、最终user、完整HTTP请求/回复、assistant输出、tools、原生反馈及来源行号/SHA256。模型文字仍有将已消耗种子/空箱笼统称为备用物资的风险，实际数量只认当前库存与原生资产。

本轮已落地设施/目标同步、采购前置流程、种植负担评估、上下文去重和原生Tool Calls。域检查564项通过；此前Python相关20项通过，本次新增报告拒绝统计后相关报告5项通过；真实API与38项原生运行契约预检通过，最终Mod编译成功，完整一天未出现ERROR/FATAL/Unhandled/Exception日志。

下一步应优先解决重复确认和回执内容的取舍：本轮context.read 16次、query.read 7次、farm.plan 6次，仍会在没有可种种子时重复求布局、对已结束商店读报价。中位样本当前状态中，前置条件2643字符、候选1993、记忆1760仍占较多。进一步优化需对同一存档做配对对照，并同时看总请求量、总输入和原生任务结果；不能只用工具数减少或API协议成功作为优化完成标准。未启动多天测试或设置长期验收通过。
