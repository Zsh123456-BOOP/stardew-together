# 三天基线第二轮实施与验证

依据：`三天基线复核与第二轮优化方案.md`。阶段 A，单玩家，Flash，原生时间。本文随验证更新；代码落地不代表原生组合或三天基线已通过。

## 修改与证据入口

| 项 | 实施 | 验证状态 |
| --- | --- | --- |
| F1 | 原始工具回执写日志；压缩阶段持有唯一 degraded_fields 集合。只有查询结果与保留字段完整相等时允许指针。world.read 的完整结果保留。容量格数、占用、约束、版本、解除条件不可省略。 | 域检查通过；首轮原生强压缩查询检查通过，52份基线请求关键标量全在；费用及空转对比见交接报告 |
| F2 | 仓库存货完成后结束当前恢复链。真正嵌套仍返回 relief_depth_limit，不登记物理约束；子恢复失败也不冒充所有候选均物理不可行。 | 纯核心及原生快照的嵌套拒绝通过；同一20次钓鱼任务完成3次原生卸货、共34件，最终任务成功 |
| F3 | tool_result 与 task_finished 使用 attempt_id；声明与执行同根因/版本去重计数。监督脚本记录现场、暂停，再退出。 | 去重/版本检查通过，第三次原生声明拒绝后实际暂停并保留现场通过 |
| F4 | 跨地图/明确出行计划开始时，将无关非保护物品一次存到出发地点可达仓库，复用 LoadoutPlan/CapacityPlan 和原生守恒核验；整备后直接续程，不返回旧站位。 | 原生卸载7件无关工具/渔获后出发，后续海滩捕获完成，前后完整快照守恒 |
| F5 | 新增原生事件获取鱼竿的依赖节点；现有邮件/事件条件、真实任务板、已开商店批准用途缺口、百科未解依赖提供候选。政策 Enabled 状态明确给模型。 | 第二轮原生读信→事件条件成立→鱼竿入包通过；采购与升级候选只提供查报价，不自动消费授权 |
| F6 | 在移动/跨图原路径邻格按原生交互拾取非保护采集物；容量模拟，单个原生行程最多3次，无路径改写。 | 第二轮 07:00 原生途经拾取珊瑚1件、对象移除；首版 Point 序列化为空，已补坐标数组，邻格坐标证据待新日志 |
| F7 | route_segment 保存目标序列、起终点和实际规划路径长度；actor_route 保存子任务、整备/存货原因；离线报表分解运输并列出重访。 | 只取证；没有修改 BFS 或新增清障寻路 |

本轮新增的是游戏适配文件，纯核心改动在已经显式注册的 ContextCompression/CapacityRelief 及现有域检查文件；没有漏注册的新纯域文件。

## 验证纪律

- 构建输出 `work/Round2Mods`，组合证据 `work/round2-combined-*`；只加载 AgentLab。
- 域检查原默认输出被已有进程占用，使用独立输出 `work/Round2Domain`，通过 `dotnet work/Round2Domain/Together.DomainChecks.dll` 执行；不杀其他任务的进程。
- 首次原生检查的鱼竿事件条件为 `739330/t 600 1710/*n spring_2_1`；没有读邀请信时原生条件不成立。未改邮件/事件标记，改的是获取路径表达，随后仍由原生阅读与剧情授予鱼竿。
- 模型原始请求/响应不含认证头；运行数据、存档、截图、控制凭据不进 Git。
- 组合不通过不启动三天；重复三次同处受阻停止。三天只从独立起点开始；G3 未设数值，14天及阶段边界不变。

## 组合尝试记录

1. `work/round2-combined-1`：F1 和嵌套限制通过；鱼竿路径缺少读信前置，未通过 F5。实际事件条件被保留，补前置后重试。
2. `work/round2-combined-2`：读真实信→原生授予鱼竿通过。同一钓鱼任务18次捕获正常完成，但三次进食腾格后仅发生一次仓库存入（12件），**未覆盖两次仓储恢复，不计 F2 通过**。没有为了通过而制造满包。
3. 第三轮明确把任务补给预算设为 `max_food=0`、目标20次捕获，检查真正两次仓储恢复；原生物资、格数、日期均不修改。第三轮通过：20次捕获，3次原生仓库存入共34件；随后整备卸载7件、海滩任务完成，第三次声明拒绝实际暂停。完整证据在 `work/round2-combined-3/`。

静态及纯核心验证：隔离构建通过；338条域断言通过；Python声明/执行根因合并及去重检查3项通过。这些不能替代组合或基线结论。

## 三天基线

组合五项通过后，已从独立新存档启动三天基线，不复用组合检查存档。完成春1后在春2 12:50发生菜单无效果阻断，观察者暂停；未完成三天，未续跑。详见[第二轮Claude交接](./三天基线第二轮-Claude交接.md)。基线使用最终构建；组合之后增加的是F7按帧运输分解、F3物理根因字段与F6坐标数组，构建和域检查再次通过。原始组合 DLL 指纹保留在其 manifest.json，基线另记实际 DLL 指纹。
