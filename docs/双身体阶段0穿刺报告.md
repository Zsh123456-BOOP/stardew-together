# 双身体阶段 0 穿刺报告

日期：2026-09-20。依据：`docs/双身体陪玩架构方案.md` 与 `CLAUDE.md`，以及本次任务的阶段 0 限定。

## 2026-09-21：采集移动补充验证（当前结论：A、B 通过）

**实测：采集移动的第一次尝试即完成 A；B 同轮再次通过，阶段 0 闸门开启。** 下方此前三次结论保留为历史事实，不覆盖原始失败证据。

**静态确认，原生距离（实现前核对）**：`work/dual-body-spike/native/Debris.cs:780–784` 在调用 collect 前检查 `abs(chunk.position.X + 32 - farmer.StandingPixel.X) <= 64` 且 Y 轴同样 `<=64`。这是方形范围，不是欧氏半径，也不是同格判断。`:476–498` 的 `collect` 本身没有距离判断；`:590–593` 的磁力半径是吸附候选条件，不能代替最终拾取距离。本探针不复制吸附，只让可见 NPC 真实移动到上述最终范围内，再同步 bot、单次作用域 collect。

本次导航与执行共用上述距离条件。导航格由 chunk 中心加上 NPC 与 Farmer 站立点偏移差求得；即使已进入目标格，只要未达拾取范围，仍交给保留的 FollowerManager 移动执行器继续走。每次锁定一个 chunk，收取成功才换下一个。每 chunk 900 tick 超时，整链 7200 tick 上限；失败证据记录 chunk ID/坐标、bot 站立点、两轴距离、剩余数量与全体仍在场的木材 chunk，不再只有一个 timeout 字符串。

---

### 本轮验收结果（实测）

证据目录 [`work/dual-body-pickup-1`](../work/dual-body-pickup-1/)，隔离运行目录 `work/DualBodyPickupMods`。仅新建 AgentLab，无模型请求、无经营存档、无跨日保存。实例已退出。

- 树实际产生四组各 3 chunk 的木材，**12 块全部原生入 bot 背包**；12 次 collect 均返回 true，调用前两轴距离都不超过 64（最大 X=63.977783，最大 Y=63.90576 像素）。没有修改掉落位置或复制吸附逻辑。
- 1360 条身体同步采样全部一致，10 次挥斧命中 `(50,25)`。10 次挥斧＋12 次拾取全部同 tick 恢复原玩家引用。可见身体有 936 个位置变化帧，按 Squad 导航完成去树、逐块拾取和返程。
- tick 1667：完整库存快照证明 bot 为 12 木材、玩家为 0；产量与入包差额 **0**。
- tick 2104：小禾已走到 `(43,28)`，玩家在 `(42,28)`。交接前 bot=12、玩家=0，交接后 bot=0、玩家=12。两包木材总量 **12→12**，所有物品按 ID＋品质汇总也完全相等。完整双方槽位快照见 `A-result.json.after.before/after` 与 `A.jsonl` 的 `handoff`。
- bot 伐木经验=14、TreesChopped=1；玩家相应值=0。未补偿或转记。
- B 本轮仍为 bot 原料50→箱子1、Chest计数0→1，玩家全部制作计数与背包不变；恢复后的 tick 与原生截图另见 `B-result.json`、`B-verify_tick-*.png`。
- `audit.json` 从原生记录核对距离、作用域、产量、交接总数；不是用域测试替代。源码快照与 `sha256.json` 同目录；证据不进 Git。

**证据范围**：只证明本树木材链和箱子制作；树液等非木材掉落、满包、跨图/跨日 Bot 生命周期和其他技能仍未验证，不称生产级双身体已完成。

### Squad Framework 冲突面清单（阶段 B 输入，静态确认）

审查对象为固定上游 Framework 的管理器、任务行为、Squad、NpcConfig、Wrappers、UI、Multiplayer 入口，并与 `build_companion.py` 暂存补丁及 `mod/SquadAdapter` 接管路径交叉核对。表中“已旁路”只指列出的路径，不表示整个类不再运行；“未旁路”是仍可到达的路径。未做阶段 B 扩面修复。

| ID / 子系统、可检索符号 | 当前状态（受管伙伴） | 会替伙伴做什么 / 冲突边界 | 旁路后必须承接的职责 |
| --- | --- | --- | --- |
| S01 `DebrisCollector.Update/TryGetTarget/CollectDebris` | 已旁路 | 自动定向掉落、公共箱/玩家入包；Harmony prefix 同时移动 chunk | 自己进入拾取范围、逐 chunk 原生入包、容量检查；本次仅木材穿刺补齐 |
| S02 `FollowerManager.UpdateSquadMember → HandleFollowing/HandleLocationAndSpeed` | 已旁路，保留前置冷却处理 | `DriveOwned` 提前 return，不再自动追玩家、跨图追赶/传送；仍用原移动执行器 | 自主目的地、步行跨图、卡住重规划；不能禁用移动执行器 |
| S03 `FormationManager.TryGetTargetTile` | 劳动主路径已旁路；类本身保留 | 只算编队格、不直接写位置；通过 S02 的 HandleFollowing 消费。`AssignSlot` 仍登记槽位 | 若要跟随编队，接管层主动选用；**不会自行改劳动站位** |
| S04 `WaitingNpcsManager` / `FollowerManager.UpdateWaitingNpc` | 列表不冲突；等待更新未旁路 | Manager 仅维护列表。进入 waiting 后每 tick Halt、锁朝向，且退出活动成员集合；**会阻止劳动** | 统一等待/恢复状态，等待中的伙伴不能派劳动；不能只改一个 stay 标志 |
| S05 `UnifiedTaskManager.FindUnifiedTask` + `TaskPriorityManager` | 已旁路 | 自动按收获/动物/伐木等优先级找任务被禁；能力查询代码仍保留 | 自己决定工作、分配目标、维护占用与前置条件 |
| S06 `FindAttackingTask` / `DriveOwnedCore` 防御分支 | 未完全旁路，适配层仍主动调用 | 会用防御任务打断非 travel 工作，配置模式对受管伙伴被改为 Autonomous | 攻击授权、优先级、中断续接与归属需统一；不能误称全部任务已由计划唯一控制 |
| S07 `OnFarmerAction/UpdateMimickingTimers/OnPlayerCaughtFish` | 部分已旁路 | 当前只保护 `IsIndependentFishing`；其他受管任务仍可被模仿计时设置或清理 | 统一手动、AI、模仿任务所有权，不能为修鱼而漏其他任务 |
| S08 `InteractionManager.AssignManualTaskFor` | 未旁路 | 热键/手动任务可直接挑最近伙伴并替换 Task；没有 IsManaged 过滤 | 保留玩家交互，但手动命令要经过统一调度、中断与材料占用释放 |
| S09 `NpcTaskBehavior.ExecuteTask` / `TaskManager.Execute*` | 部分适配，未整体旁路 | Before/AfterTask、动画冲击、CargoAccept 已接；仍大量调用旧任务执行与原料产物逻辑 | 阶段 B 逐技能迁移原生 Farmer 调用、真实体力/工具，不能关闭后只剩动画 |
| S10 `TaskManager.TryAddItemToInventory/CanAcceptItem` | 部分旁路 | 活跃 OwnOutput 范围可走 Together 货袋；其他路径仍走公共箱/玩家/地面 | D6 唯一库存、容量、叠堆、余量、交接、持久化；当前不等于全面完成 D6 |
| S11 `SquadInventoryMenu` / `InteractionManager.ShowSquadInventory` | 未旁路 | 菜单仍读写招募者公共箱，不是 BotFarmer.Items | 保留互动入口但明确所展示库存；阶段 B 提供伙伴真实背包交接 |
| S12 `FollowerManager.WarpSquadToFarmer` / `BlockRecoveryWarp` | 已旁路 | 过场结束追赶传送、卡住兜底传送已针对受管伙伴拦截 | 正常地图出口行走、可解释卡住结果，不能再靠传送完成劳动 |
| S13 `UpdateRidingState/UpdateRidingMember` | 未旁路 | 发生在 DriveOwned 之前，自动选伙伴同乘、清任务、逐帧写位置，跨图 warp | 同乘须由调度显式允许；上下马与劳动所有权协调；本轮无马未触发 |
| S14 `RecruitmentManager.OnDayEnding/DismissAll/DismissAllWaiting` | 未旁路；NPC dismissal 位置有保护 | 原生招募保留，但日末会移出成员、清状态；`KeepDismissalPosition` 只阻止部分 warp | 跨日恢复管理身份、任务和 bot 库存，不应把位置保护误当生命周期完整 |
| S15 `FollowerManager.HandleFestivalState/HandleOfflineRecruiters` | 未旁路 | 节日自动解散；多人离线自动 waiting/解散/恢复 | 节日让步策略、断线/恢复所有权；单机无离线分支但不能据此称安全 |
| S16 `SquadMateStateHelper.MaintainControl` | 不冲突，保留 | 清原生 NPC controller/日程方形走动，允许玩家穿过伙伴；不会直接选择劳动 | 必须保留以防原生日程抢身体；未来不能另外依赖 npc.controller 作为导航而不协调 |
| S17 `BehaviorManager.PlayIdleAnimation` / 动画冷却 / `SpriteManager` | 自动闲逛动画入口主路径已旁路；外观不冲突 | 主调度 return 后不随机 idle；冷却、动画播放和网络表现仍运行 | 自主休息/表情可主动调用；劳动中动画与冷却需协调，不删外观系统 |
| S18 `HandleFriendshipGain` | 未旁路，与劳动站位无直接冲突 | 每小时向招募者加原生好感，另有 Together 关系状态 | 若将来统一关系规则需避免重复增长；关闭后要明确定义陪伴关系来源 |
| S19 `MessageDispatcher` 请求 / 快照应用 | 未旁路；转发上述入口 | 手动派工、waiting、解散、模仿从网络进入；客户端快照替换成员和表现状态 | 同一授权/所有权规则用于本地和网络；本轮单机不作为联机安全证明 |
| S20 `NpcInteractionBehavior/PetInteractionBehavior`、UI Prompt/Menu | 交互本身不冲突，保留 | 招募、对话、靠近互动保留；wait/dismiss/inventory 的动作仍可碰 S04/S11/S14 | 保留入口，动作接统一控制；不把整个交互系统删掉 |
| S21 `AssetManager/NpcConfigManager/BaselineContentLoader`、Models、只读/随机 Wrappers、`ServiceFactory/PetOwnerResolver/SquadMateFactory` | 不冲突 | 数据装配、身份查询、条件/纹理/对话，不自行取物或排劳动 | 不需要替代；位置/能力查询的 Game1.player 依赖仍按 B 清单审查 |

**额外必须登记的归属风险（静态确认，未在穿刺执行）**：上游 `TaskManager.TransferExperienceGained:288–298` 会直接减 source.experiencePoints，再向 recruiter 加经验；`ExecuteHarvestingTask:1593–1594` 调用它。`ExecuteForagePickup:1819`、动物产物 `:1848/1852`、挤奶剪毛 `:3297` 也自行调用 gainExperience。阶段 B 不能把这些路径当作 D5 原生行为直接复用，更不能复用转记代码。此问题与本次作用域原生挥斧所得 14 XP 不同。阶段 A 不新增此类写入，不以这些伙伴行为证明零伪造能力。

保留项按用户决定：AStarPathfinder 寻路、FollowerManager 移动执行、NPC 外观/对话/招募/靠近互动全部保留。上述表列出的是**有限接管边界**，不是建议关闭每个系统。

阶段 B 原生依赖检索入口：[双身体阶段B原生依赖索引](./双身体阶段B原生依赖索引.md)。

---

## 2026-09-21：按 D6 排除接管后的第三次尝试（历史结论）

**A 未通过，第三次尝试已停止，不做第四次修补；B 再次通过。阶段 A 未开始，14 天验收未启动。** 作用域内原生拾取本次确实成功了，但只收取 1 块木材，后续移动与拾取衔接停滞，最终超时。没有走回玩家，没有交接，不能宣称阶段 0 闸门开启。

### 本次修改与隔离

- 重读新增 D6 的架构方案。按 `scripts/build_companion.py:39–49` 的精确锚点补丁，在暂存的 Squad `DebrisCollector` 三处排除受管伙伴：扫描候选、读取已有掉落目标、最终写入公共库存前检查。使用 `CompanionControl.IsManaged(mate)`；非受管伙伴保留原路径，没有修改 `external/` 克隆。
- **实测配置**仍是 `EnableGathering=true`、`UseSquadInventory=true`，没有靠全局开关绕过。
- 挥斧与拾取仍分别使用同步 `ActingFarmer.As(bot)`；没有替换 `Debris.collect`、没有使用 fallback、没有跨帧作用域。
- 新增只读产量记录：对每组首次观测到的原生木材 debris 记录 chunk 数、位置、类型，再累计每次原生拾取后 bot 背包的实际增量。没有向 A 的任一背包添加木材。
- 独立运行目录 `work/DualBodyD6Mods`；新建 AgentLab，save_id `449597501`，桥接端口 18767。三重夹具门禁不变，没有运行经营存档，没有模型调用，没有原生过夜保存。
- 构建通过，Python 语法检查与 `git diff --check` 通过。这些不替代以下原生证据。

### 原生证据

证据目录：[`work/dual-body-d6-spike`](../work/dual-body-d6-spike/)。

| 项目 | 证据强度 | 本轮结果 |
| --- | --- | --- |
| B 原生制作 | 实测 | bot 50 木材→1 箱子，Chest 计数 0→1；玩家完整背包与全部制作计数字典不变 |
| B 恢复与渲染 | 实测 | 作用域 tick 11854，验证 tick 11874；3 次渲染身份检查正确；已查看 `B-verify_tick-11864.png`，玩家仍在住宅床上，无隐形 Farmer 或菜单串屏 |
| A 身体同步 | 实测 | 7200 条位置、朝向、场景引用一致的同步记录 |
| A 挥斧 | 实测 | 10 次原生挥斧，全部命中树格 `(50,25)`；原子作用域同 tick 恢复玩家 |
| A 原生归属 | 实测 | bot 伐木经验 14、TreesChopped=1；玩家相应经验与计数仍为 0，没有补偿或转记 |
| 树倒下与产量 | 实测 | tick 12444 树倒下；观测到 4 组木材 debris，各 3 个 chunk，总计 **12 木材单位** |
| 作用域内拾取 | **实测成功 1 次** | tick 12567，`Debris.collect` 返回 true；bot 木材 0→1，玩家仍为 0；同 tick 退出恢复原玩家 |
| 后续拾取 | 实测未完成 | 从 tick 12582 起，最后 6508 条同步记录位置不变；没有第二次 collect 调用 |
| 超时停止 | 实测 | tick 19089，`LabDualBody.cs:78` 抛出 `probe_timeout_120_seconds`，原玩家引用正确 |
| 交接与守恒 | **未验证** | `handoff` 事件为 0；玩家仍无木材，bot 为 1，不能制作交接前后已通过的结论 |

**多 chunk 数量核对**：本次观察到的产量为 12，实际入 bot 背包为 1，差 **11**。这是“尚未完成/核验拾取的差额”，**不是已证明丢失了 11 块木材**。本次没有在终点逐块快照剩余 debris，不能断言它们全部仍在地上，也不能说产量已完整守恒。四组各 3 chunk 的原生记录证明必须按 chunk 核对，不能把一个 debris 当成一件物品。

原生拾取已从 bot 0→1，且玩家背包与 A 开始前完全一致。这提供了“木材确实先进 bot”的正向证据，**并未提供交接守恒证据**。

### 第三个阻断：定位到哪里，哪里仍不确定

**实测定位**：最终停在 `pickup`，小禾和 bot 都位于像素 `(3361.1013,1601.156)`。世界读取的伙伴格为 `(53,25)`，`moving=false`。第一次拾取成功后没有下一次 `atomic_scope(operation=collect)`，没有原生 collect=false，也没有新的身份门槛异常。结束原因是总探针超时，不能把它写成“作用域拾取再次遭到拒绝”。

**静态确认的停滞相关边界**：

1. `mod/Together/LabDualBody.cs:178–181` 用 **bot.StandingPixel 与 chunk 像素距离**决定是否允许调用 collect；超出任一轴 64 像素就继续导航并 return。
2. 同处导航目标是 `(chunk.position / 64).ToPoint()`；只有目标格改变才重新调用 `DualWalk`。
3. `mod/SquadAdapter/OwnedMovement.cs:67–69` 则用 **npc.TilePoint 等于目标格**判定到达并 Halt。两个到达判定并非同一个条件，也没有“已到格但不满足拾取距离”的纠偏/进度检查。

**待验证**：日志未记录停滞时当前被选 chunk 的精确位置、`dualPickupTarget`、路径队列和 Halt 分支，所以本轮能确定“导航到拾取的衔接没有继续推进”，但**不能确定**到底是格/像素到达判定不一致、路径结束、障碍，还是同格目标未重派。没有为补齐这些插桩再启动第四次，也没有更换拾取实现。

这属于执行衔接层的新阻断。它没有证伪 D5 的原生拾取身份切换，但使 A 的整条链仍不能验收。

### 三次阻断按类型汇总

| 尝试 | 类型 | 直接证据 | 当前认识 |
| --- | --- | --- | --- |
| 第一次 | 原生行动者身份上下文 | 作用域外 collect=false；`IsLocalPlayer` 门槛 | 第三次作用域内 collect=true，已取得该修法的一次正向证据 |
| 第二次 | 两个库存/两条拾取路径竞争 | collect 未执行；Squad 公共箱有木材与树液，bot 为空 | D6 排除受管伙伴后已观察到 bot 原生入包；全库存迁移仍是阶段 B，未做 |
| 第三次（本轮） | 导航与逐 chunk 拾取的连续执行 | 成功拾取 1 次后位置长期不变，最终 timeout；未返回交接 | 不新增第四轮修补；完整原因仍需更细目标/路径证据，交由用户重新判断方向 |

### 停止与留档

- 已退出本次隔离实例；没有操作原有其他实例，没有进入阶段 A/B，也没有启动 14 天运行。
- 未进行第四次 A 尝试，未扩大到生命周期、货袋迁移或能力移植。
- 证据包含 `A/B-result.json`（完整槽位、配方与经验快照）、`A/B.jsonl`、采样 JSONL、`wood_spawn_observed` 与 `native_collect` 事件、`failure-world-state.json`、`failure-world-read.json`、`failure-map.json`、原生 B 截图、`audit.json`、源码快照 `source/` 和 `sha256.json`。A 没有交接后的快照，因为交接没有发生。
- 构建日志 `work/dual-body-d6-build.log`；运行数据全部留在被忽略的 `work/`，不入 Git。
- 按仓库“阶段通过验证后才提交”的纪律，本次未做阶段通过提交。修改与证据均保留。
- 用户指定的“更新 A 为通过并整理阶段 B 依赖检索表，然后直接进入阶段 A”前提尚未成立。原报告 §5.1 依赖清单继续完整保留，未宣称已经迁移或验收。

---

以下为前两次历史记录。当前结论以上述第三次尝试为准。

## 2026-09-21：按 D5 修复后的第二次复测（历史结论）

**A 仍未取得完整证据，阶段 0 闸门仍关闭；没有开始阶段 A、没有运行 14 天验收。B 本轮再次通过。** 本次按用户要求将挥斧与拾取分别包进同步行动者作用域，没有改用 Farmtronics 的非 bool 入包入口，没有转记经验或统计。

这次失败与首次失败不同：**不是作用域内的 `Debris.collect` 被拒绝，而是该调用尚未执行，掉落物已经被另一条拾取路径接管。** 不据此认定 D5 无效，也不宣称交接守恒已经通过。按“穿刺 A 修复后仍不通过则停下”规则，只进行了一次本轮原生复测，失败后只读取库存、核对源码并保存证据，没有调整拾取配置后继续试。

### 本次改动

- `LabDualBody.cs`：一次 `GetToolLocation + Axe.DoFunction` 在一个 `ActingFarmer.As(bot)` 中完成；一次 `Debris.collect` 在另一个作用域中完成。路径移动、树倒下动画等待、往返导航都在作用域之外。
- 每个实际执行的原子作用域记录进入/退出 tick 和恢复的玩家引用。
- 只读快照增加 `experiencePoints` 和 `TreesChopped`，不写入或补偿任何进度。
- 交接准备了玩家与 bot 的完整槽位快照，以及所有物品按 ID＋品质聚合的数量相等断言；但本次未执行到交接，这些断言**尚未得到原生通过证据**。
- 检查脚本支持独立证据输出目录。旧运行目录及旧证据未覆盖。

### 原生结果

运行目录 `work/DualBodyD5Mods`，新建 AgentLab，证据目录 [`work/dual-body-d5-spike`](../work/dual-body-d5-spike/)。

| 项目 | 证据强度 | 本次结果 |
| --- | --- | --- |
| B 原生箱子制作 | 实测 | 再次通过；隐形计数 0→1，玩家完整制作字典与背包不变；作用域 tick 318，后续验证 tick 338，3 个渲染回调检查 |
| A 身体同步 | 实测 | 803 个同步采样一致，10 次挥斧仍命中 `(50,25)` |
| A 挥斧作用域 | 实测 | 10 次均同 tick 进入退出，全部恢复玩家原引用 |
| 隐形 Farmer 原生伐木经验 | 实测 | 0→14，玩家仍为 0 |
| 隐形 Farmer `TreesChopped` | 实测 | 0→1，玩家仍为 0 |
| A 原生树倒下 | 实测 | tick 909 记录 `stump=true, falling=false` |
| A 拾取作用域 | **未执行** | `atomic_scope` 中 operation=collect 的记录为 **0** |
| A 隐形背包木材 | 实测 | 0；仍只有斧头和 11 个空槽 |
| 玩家背包 | 实测 | 与 A 开始前完全一致，无木材 |
| Squad 公共背包 | 实测 | 失败后只读查询看到 **4 木材、5 树液**；不是 Together 伙伴货袋，也不是 bot 背包 |
| A 交接与守恒 | **未验证** | 未发生返回交接，不能以空背包总量相等判通过 |

### 阻断位置与原因

探针在 **tick 1157**、`mod/Together/LabDualBody.cs:165` 抛出：

```text
fallen_tree_produced_no_collectable_wood
```

该错误名不够准确。它对应的实际判定是：**当前追踪的木材 debris 已不在场景中或没有 chunk，而 bot 木材数量仍为 0**；不能据错误名称认定“树没有产生木材”。本次从 tick 909 到失败前仍有走向掉落位置的过程，且第三个库存实际存在木材。

**实测配置**：`EnableGathering=true`、`UseSquadInventory=true`，见 `squad-pickup-config.json`。本轮没有更改这两个设置。

**静态确认的冲突路径**：

1. `external/the-stardew-squad/TheStardewSquad/Framework/DebrisCollector.cs` 的 `Update` 每 30 次更新扫描 NPC 附近掉落，放入 `_targetedDebris`。
2. `HarmonyPatches.cs:383` 的 `Debris_UpdateChunks_Prefix` 检查目标；命中后自行吸向 NPC，`:416` 调 `CollectDebris`，`:420` 跳过原生更新。
3. `DebrisCollector.cs:141–151` 在 `UseSquadInventory` 为 true 时将物品放入 `TheStardewSquad_SquadInventory_<玩家ID>`。
4. `DebrisCollector.cs:178–181` 在接收后删除整个 debris。因此随后 Together 探针扫描不到它，无法进入自己的 `ActingFarmer.As(bot) → Debris.collect`。

**证据边界**：第三个库存有木材/树液是实际读取结果；上述接管与删除是代码直接存在的路径，本轮没有对 Squad 每一个 chunk 的迁移做动态插桩。因此不能把 4 木材宣称为树的完整产量，更不能据此证明掉落物数量守恒。上游 `CollectDebris` 对没有 item 的 debris 创建数量 1 的对象、随后移除整个 debris，也提示多 chunk 数量需要单独核对；本轮不扩展修复。

这是**两条拾取控制路径的冲突**，尚不是“作用域内 IsLocalPlayer 仍失败”的证据。作用域内拾取是否通过，仍待一次没有竞争拾取者的原生验证。

### 停止状态、证据和下一次决策点

- 隔离实例已退出；没有启动阶段 A，没有新增正式经营/夜间守护/失败降级代码，没有进入阶段 B。
- 未启用 fallback；未把公共背包物品搬到 bot 来制造通过证据；未手动增加任何经验、计数或材料。
- 本轮阶段 0 仍失败，继续遵守“通过后提交”的仓库纪律，未做通过性提交。
- 完整证据包括 `A-result.json`、`A.jsonl`、`B-result.json`、`B.jsonl`、`failure-world-read.json`、`failure-world-state.json`、`audit.json`、`sha256.json`。相关运行源码副本在 `source/`。编译日志 `work/dual-body-d5-build.log`。
- 如果继续验证，最小候选是**仅让实验中的小禾不再被 Squad 自动拾取器接管掉落**，仍保持 `ActingFarmer.As(bot) → Debris.collect` 原路径和原生检查。上游排除逻辑须按 `build_companion.py` 锚点补丁＋`mod/SquadAdapter/` 方式实现，或由明确的隔离夹具配置控制。本轮仅记录此候选，没有实施或重试。
- A 尚未通过，所以用户要求的“更新为 A 通过、整理阶段 B 检索清单后直接进入阶段 A”条件没有成立。下方首次报告 5.1 的依赖清单继续保留，没有冒充阶段 0 已验收。

---

以下保留 **2026-09-20 首次穿刺的原始报告**；其中 tick、行号、证据目录均属于旧版本。当前结论以上述 D5 复测为准。

## 首次穿刺结论

**阶段 0 整体未通过，已停止。穿刺 B 通过本次限定的原生制作检查；穿刺 A 未通过，卡在真实掉落物进入隐形 Farmer 背包。没有进入阶段 A 或任何后续阶段，没有在失败后替换拾取路径或重跑。**

| 穿刺 | 结论 | 已取得证据 | 未取得证据 |
| --- | --- | --- | --- |
| A：NPC＋隐形 Farmer 砍树、拾取、交接 | **未通过** | Squad 移动小禾；680 个同步采样一致；10 次原生挥斧命中同一棵树，树确实倒下 | 首次原生拾取被拒绝；隐形背包木材为 0；未执行返回交接，无法证明交接守恒 |
| B：单 tick 切换行动者、原生制作箱子 | **通过，范围仅为这一次箱子制作** | 隐形背包 50 木材→0 木材＋1 箱子；隐形制作计数 0→1；玩家背包及全部制作计数不变；作用域与后续 tick 引用正确；渲染身份检查和制作后截图正常 | 未证明所有工具、所有菜单、所有 mod 组合和长期运行都安全 |

A 的失败**不等于已经证伪整个双身体架构**。当前尝试使用 `Debris.collect(bot, chunk)`，它有原生本地玩家门槛；Farmtronics 的自定义拾取走的是另一条入口。是否改用该入口，或在拾取时使用行动者作用域，留待方向决策。本轮没有实施这些候选调整。

## 1. 环境、隔离与证据目录

- **实测**：Stardew Valley `1.6.15.24356`、SMAPI `4.5.2`。
- 新建 AgentLab，运行中读取到玩家名 `AgentLab`，初始游戏日 0。没有加载经营存档。
- 独立运行目录 `work/DualBodyMods`，本地桥接端口 `18767`。只加载 Console Commands、AgentBridge、Squad、Together。
- 未向 DeepSeek 发请求。AI 暂停，Together 普通业务循环在穿刺期间不执行。
- 已退出本次新启动的隔离游戏实例；没有关闭或修改原先的其他游戏实例。未执行过夜或显式保存测试结果。
- fixture/start 入口检查 `Settings.EnableLab`、世界就绪、`Game1.player.Name=="AgentLab"`，并拒绝多人；Squad lab 导航入口也检查 lab 配置、世界就绪和玩家名。
- B 仅向隐形背包提供 50 木材作为明确的材料夹具。A 的夹具清出农场一小块测试区域并放置一棵原生成树；不向任一背包提供木材。初始化位置是夹具设置，之后测试行走由 Squad 完成。
- 没有编写任何直接修改 Farmer 经验、配方、成就、金钱或统计计数的赋值。默认配方来自原生 `Farmer` 构造流程内的 `LearnDefaultRecipes()`；箱子的制作计数由原生制作处理器增加。

原生结果集中在 [`work/dual-body-spike`](../work/dual-body-spike/)，不进入 Git：

| 文件 | 内容 |
| --- | --- |
| `metadata.json` | 对当前游戏程序集的运行时反射结果 |
| `initial-state.json` | 新建 AgentLab 的初始状态 |
| `B-result.json`、`B.jsonl`、`B-samples.jsonl` | B 的前后背包、配方计数、作用域内外身份、后续 tick、渲染检查 |
| `A-result.json`、`A.jsonl`、`A-samples.jsonl` | A 的同步轨迹、挥斧坐标、树状态、失败堆栈、背包快照 |
| `native-evidence-audit.json` | 对上述原生记录的离线一致性核对，不是单元测试替代品 |
| `B-verify_tick-1050.png` | 制作完成、作用域退出后的原生世界渲染 |
| `B-craft-1038.png` | 制作前帧；仍处于开局淡入黑屏，不能单独作为视觉对比基线 |
| `A-walk_tree-1076.png`、`A-failure-world.png` | A 的场景截图；失败截图为停止后采集，不是拾取成功证据 |
| `native/*.cs` | 从当前安装的原生 DLL 只读反编译的相关类，用于定位依赖和失败行 |
| `sha256.json` | 证据文件校验值 |

原始逐帧输出还保留在 `work/DualBodyMods/Together/dual-body-evidence/`。构建日志是 `work/dual-body-build.log`。

## 2. 反射复核与作用域原语

**实测**：先运行只读元数据探针，再实现作用域。

```text
public_set       = false
nonpublic_set    = Void set_player(StardewValley.Farmer)
field            = StardewValley.Farmer _player
field_private    = true
field_static     = true
field_readonly   = false
field_matches_player = true
farmer_sealed    = false
```

**静态确认**：`Game1.player` 的非公开 setter 除了赋值，还执行旧玩家的 `unload()`，并设置新背包的 `IsLocalPlayerInventory`。本探针只反射切换 `_player`，不调用 setter，不冒用玩家 ID。

实现位于 [`LabActingFarmer.cs`](../mod/Together/LabActingFarmer.cs)：

- 仅由 AgentLab 探针调用的内部类型，未接入正式工具或经营循环。
- 进入时拒绝已有菜单、剧情和嵌套作用域；记录原 Farmer 引用、线程和 `Game1.ticks`。
- 使用 `using` 同步进入和退出，无 `await`，原生制作菜单不赋给 `Game1.activeClickableMenu`。
- `Dispose` 恢复原引用，断言引用、线程、tick 均正确；异常由探针捕获，写出失败堆栈和结果并暂停。
- 渲染回调再次检查 `Game1.player` 身份；B 随后的 update tick 继续核对玩家背包及配方计数。

**待验证**：异常注入、世界卸载与更多 mod 同步回调等情况，本轮没有扩大验证。这里是穿刺原语，不是正式生命周期设计。

## 3. 穿刺 B：原生制作结果

先执行 B，以尽早验证方案标记的主要风险。代码入口：[`LabDualBody.cs:82`](../mod/Together/LabDualBody.cs#L82)。

执行路径：

```text
原生 Farmer 构造（默认配方）
→ 隐形背包夹具放入 50 木材
→ using (ActingFarmer.As(bot))
→ new CraftingPage(...)
→ 找到原生 Chest 配方组件
→ 反射调用私有 clickCraftingRecipe(component, false)
→ 原生 heldItem 经 addItemToInventory 进入隐形背包
→ 退出作用域
→ 后续 tick 与渲染身份检查
```

这里没有手工扣 50 木材，没有手工创建箱子，没有手工增加制作计数。

**实测结果**：

| 检查 | 制作前 | 制作后 |
| --- | --- | --- |
| 隐形 Farmer 木材 | 50 | 0 |
| 隐形 Farmer 箱子 `(BC)130` | 0 | 1 |
| 隐形 Farmer `craftingRecipes["Chest"]` | 0 | 1 |
| 玩家 `craftingRecipes["Chest"]` | 0 | 0 |
| 玩家全部 `craftingRecipes` | 原始字典 | 完全一致 |
| 玩家背包，包括槽位、ID、品质和数量 | 初始工具等物品 | 完全一致 |
| `Game1.player` | 玩家本人 | 作用域内为隐形 Farmer，退出后为原玩家 |

- `inside_scope` 与 `after_scope` 都发生在 **tick 1039**。
- B 的通过记录发生在 **tick 1061**，已越过至少一个完整 tick，原玩家引用仍正确。
- B 通过前记录 **3 个渲染回调**，均读到原玩家 ID，没有渲染隐形 Farmer。
- 作用域后检查玩家渲染器引用、当前场景引用、viewport 均未改变，未出现可交互制作菜单。
- 已人工查看 `B-verify_tick-1050.png`：玩家仍在新存档住宅的床上，未出现第二个 Farmer 或制作界面串到屏幕。制作前截图处于淡入黑屏，因此本结论结合引用断言、逐帧身份检查和制作后截图，不把黑屏当作有效的前后像素对照。

**结论：B 达到此次指定的一次箱子制作门槛。** 它没有证明“原生代码不依赖 Game1.player”；恰恰相反，本次作用域把这些依赖统一指向了隐形 Farmer。

## 4. 穿刺 A：已跑到哪里，卡在哪一行

**实测**：

1. 小禾沿 Squad 导航从夹具起点走向树前，不是用瞬移完成劳动路线。
2. 同步记录共 **680 条**，NPC 与隐形 Farmer 的 `Position`、朝向及 `currentLocation` 引用均一致。
3. 挥斧时两个身体的位置均为 `(3168,1664)` 像素、朝上；`GetToolLocation(true)` 为 `(3200,1616)`，对应目标树格 **`(50,25)`**。
4. **10 次**调用真实 `Axe.DoFunction(location,x,y,1,bot)`，每次坐标均是目标树；树健康值递减，最终原生状态变为树桩并进入倒树过程，随后 `falling=false`。
5. 原生树产生了可拾取木材，小禾继续走向掉落位置。失败时隐形 Farmer 在 `(52,25)` 附近，背包只有斧头和 **11 个空槽**，体力从 270 降到 250。
6. 第一次调用 `Debris.collect` 返回 `false`。玩家背包仍与 A 起始快照一致；隐形背包木材数量 **0**。

失败位置：[`mod/Together/LabDualBody.cs:171`](../mod/Together/LabDualBody.cs#L171)。

```csharp
if(!debris.collect(bot,chunk))
    throw new InvalidOperationException("native_debris_collect_rejected");
```

**实测失败 + 静态确认原因**：

```text
Debris.collect(bot, chunk)
  → Farmer.addItemToInventoryBool(item)
    → if (IsLocalPlayer) { ...真正加入背包... }
    → return false
```

当前原生 DLL 的对应位置：

- `work/dual-body-spike/native/Debris.cs:492`：将真实掉落物交给 `farmer.addItemToInventoryBool`，失败则返回 false。
- `work/dual-body-spike/native/Farmer.cs:4395`：`if (IsLocalPlayer)` 门槛。
- 同文件 `:4420`：非本地玩家路径返回 false。
- 同文件 `:1877`：`IsLocalPlayer` 比较自身与 `Game1.player.UniqueMultiplayerID`；剧情角色还有特殊分支。本次不在剧情中，两个 ID 不同。

因此这次不是背包满、没有扫到掉落物或工具砍错位置。**隐形 Farmer 虽然是合法 Farmer，但在作用域外不被原生 bool 拾取接口视为本地玩家。**

没有执行后续“走回玩家→转交木材”。交接代码存在，但未执行，**不能宣称守恒验收通过，也不能以两个背包都没有木材冒充守恒证据**。

### 与 Farmtronics 参考实现的关键差异

**静态确认**：`vendor/Farmtronics/Farmtronics/Bot/BotObject.cs:690` 附近的 `PickUpDebris` 自行吸引掉落物，在到达距离内后调用 **`farmer.addItemToInventory(item)`**，而非 `Debris.collect` 使用的 **`addItemToInventoryBool`**。两者不是等价入口：原生 `addItemToInventory` 本身没有上述 `IsLocalPlayer` 外层门槛。

本次选择 `Debris.collect` 是为了尽量保留原生掉落转换过程；实测暴露了这个入口差异。A 的失败属于当前穿刺路径的明确阻断，不足以推出 Farmtronics 模式不可行。

**未实施、待决策**：严格核对返回余量后复用 Farmtronics 的拾取方式，或评估单 tick 行动者作用域能否也覆盖拾取。失败发生后没有修改这段代码、重试或继续扩面。

## 5. 已发现的 Game1.player 隐式依赖清单

下面均来自原生代码的**静态确认**。B 实测只覆盖其中箱子制作实际走过的分支，不代表每个分支都经过动态插桩。

### 5.1 制作与菜单

原生反编译文件位于 `work/dual-body-spike/native/`。

| 位置 | 全局依赖及影响 | 本轮覆盖 |
| --- | --- | --- |
| `InventoryMenu.cs:90–96` | 默认绑定 `Game1.player.Items`，按当前玩家容量补空槽 | B 构造制作菜单会经过，故必须连构造都放在作用域内 |
| `CraftingPage.cs:87` | 垃圾桶等级从全局玩家读取 | B 构造 |
| `CraftingPage.cs:115` | 配方列表按全局玩家的已知制作配方过滤 | B 构造、列出 Chest |
| `CraftingPage.cs:233` | 烹饪配方是否已知影响显示 | 烹饪未验证 |
| `CraftingRecipe.cs:136、140` | 读取当前配方制作计数、捕蟹笼职业条件 | 前者属于 B 构造；职业分支未验证 |
| `CraftingRecipe.cs:164–171` | 原料数量来自全局玩家和可选容器 | 候选/交互路径；未单独插桩 |
| `CraftingRecipe.cs:280–286` | 消耗 **Game1.player.Items** 中的材料，没有显式 who 参数 | B 原生消耗 50 木材 |
| `CraftingPage.cs:485` | 对全局玩家触发 `NotifyQuests(OnRecipeCrafted)` | B 原生处理器 |
| `CraftingPage.cs:486–488` | 增加全局玩家的 `craftingRecipes[recipe]` | B 证明增加的是隐形 Farmer 的计数 |
| `CraftingPage.cs:497` | `Game1.stats.checkForCraftingAchievements()` | B 原生处理器；此处 `Game1.stats` 本身指向 `player.stats` |
| `Stats.checkForCraftingAchievements` | 从 `Game1.player.craftingRecipes` 汇总，原生写入自身 itemsCrafted 并可能发成就 | 本次只做第一个箱子，未验证达成成就阈值的分支 |
| `CraftingPage.cs:499–501` | 手柄模式下把产物加入全局玩家背包 | 本次不是该分支；探针把 heldItem 交给明确的 bot 背包 |
| `CraftingPage.cs:404–406、418` | Shift 入包、丢弃 heldItem 都读取全局玩家 | 未调用玩家 UI 操作路径 |
| `CraftingPage.cs:588–617` | 回收价、垃圾桶渲染读取全局玩家 | 实验菜单未显示，未验证 |
| `InventoryMenu.cs:262、409–449、531–545` | 回调、停止持有动作、当前工具与容量显示读取全局玩家 | 未验证这些交互/绘制分支 |
| `CraftingRecipe.cs:205、340–394、468–532` | 特殊烹饪规则、额外原料/调味料、配方提示与历史读取全局玩家 | 未验证烹饪/提示分支 |
| `CraftingPage.cs:492–493` | `cookedRecipe` 与烹饪成就检查指向全局玩家 | 未验证烹饪 |

这些已发现的路径没有被改写或 Harmony 绕过。本次只是把原生制作调用完整放入同步作用域。它们说明 **“只把 DoFunction 的 who 换成 bot”不能自动推广到全部能力**。

### 5.2 本次 A 还暴露的依赖

- **静态确认**：`Character.GetToolLocation` 即使接收 `ignoreClick`，仍有检查 `Game1.player.CurrentTool` 与玩家格坐标的分支；本次 A 的实际 10 次工具坐标正确，其他位置组合未验证。
- **实测失败＋静态确认**：`Debris.collect`→`addItemToInventoryBool` 的本地玩家门槛，见上节。
- **静态确认**：`Debris.findBestPlayer` 遍历 `location.farmers`；一个未注册到玩家集合的隐藏实例不会自动进入原生吸附候选。本次使用显式跟踪＋调用 collect，未伪装成联机玩家。
- **静态确认**：`Tree` 的掉落、倒向、额外木材等有 `Game1.player` 引用；部分路径通过 `Game1.GetPlayer(lastPlayerToHit)` 查行动者，查不到回退 `Game1.MasterPlayer`。隐藏实例未注册时存在归属/加成风险。
- **静态确认**：`Tree.performTreeFall` 的经验及 `TreesChopped` 原生增长还受 `getLastFarmerToUse().IsLocalPlayer` 限制。本次没有为补偿这些路径写经验、计数或转记，也没有把它们标成已验证的独立成长能力。

## 6. 改动范围与验证记录

仅增加/接线以下实验内容：

- `mod/Together/LabDualBody.cs`：元数据探针、两个穿刺状态机、背包与逐帧日志、截图。
- `mod/Together/LabActingFarmer.cs`：lab 使用的同步作用域。
- `mod/Together/LabScenarios.cs`：三个 `dual_body_*` lab 入口。
- `mod/Together/ModEntry.cs`：lab 接口、实验 update/render 回调。
- `mod/SquadAdapter/LabDualBodyMotion.cs`、`OwnedMovement.cs`：仅在实验指令存在时使用的 Squad 行走目标。
- `scripts/check_dual_body_spike.py`：顺序 B→A，遇到首个原生失败立即终止，不自动重试。

没有改 `external/`、没有迁移正式工具、没有改调度/经营/记忆，没有推进阶段 A 及以后。新文件是依赖游戏程序集的运行时实验代码，**没有新增纯域文件或域测试**，因此没有伪称已纳入 `Together.DomainChecks`。

验证记录：

1. 构建隔离运行时成功；Together 0 错误、0 警告；Squad 上游构建仍有既有 nullable 等警告。
2. Python 检查脚本语法检查通过；`git diff --check` 通过。
3. 本轮只执行了一次两链原生检查：B 通过，A 在首次拾取失败后停止。编译阶段的类型修正不属于原生重跑。
4. 失败后仅做只读代码核对、证据汇总和截图，随后退出隔离实例，没有更换拾取算法再试。
5. **阶段未通过，遵守 CLAUDE.md 的阶段验收后提交规则，本轮未做通过性提交。** 未暂存用户已有的 `CLAUDE.md`、架构方案或 `ProgressGraph.cs`。

复现命令（会在新建 AgentLab 中执行夹具；本报告交付后没有自行重跑）：

```bash
python3 scripts/build_companion.py --mods-dir work/DualBodyMods
python3 scripts/launch.py --companion --lab --mods-dir work/DualBodyMods --port 18767
# 新实例标题界面的开发控制台：agent_new
# 等待进入 AgentLab 后，另一个终端：
python3 scripts/check_dual_body_spike.py
```

脚本及运行时记录会保留首个失败。再次复现应先归档旧的 `work/dual-body-spike` 和运行时 evidence 目录，以免混淆不同运行记录。

**当前决策门槛仍关闭：A 的拾取与跨背包守恒未完成，不据此启动后续架构迁移。**
