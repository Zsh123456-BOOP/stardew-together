# 第七轮：农场清障与拾取衔接

2026-09-22。本轮依据用户“修复完然后重新启动 7 天测试”的授权，将已成为真实阻断的农场清障提前实施。此授权不等于阶段 B 开放，也不豁免独立 14 天闸门。

## 根因

上一轮 `work/u6-trial-7days-2` 在春 1 11:50 停止，未完成一天。砍倒 Farm(70,23) 的树后，9 块木材散落到杂物围住的区域。原生移动只能通过可走节点；随后的采石和采木任务又将旧掉落拾取作为强制前置，连续三次 `pickup_unreachable`。没有改失败监控阈值。

## 实现（静态确认）

- `ClearanceRouting.cs`：纯算法比较行走与原生清障成本；缺少工具、受保护对象、体力不足、掉落容量不足均不能当作可穿越节点。沿单次原生行动最多清除三个挡路目标，防止导航变成无限清图。新域文件显式注册。
- `PlayerClearance.cs`：仅在农场支持普通石头（343/450）、树枝、已识别普通杂草。成熟树、幼树、巨石/树桩、建筑、作物、设备和未知对象不由导航擅自处理。原有明确伐木工具仍然可用。
- 沿已有控制器移动至相邻站位 → 原生 `BeginUsingTool` → 核验对象变化与真实碰撞解除 → 继续原路线。不删除地图对象、不瞬移、不直接入包。跨图清障绑定实际清障地图，而非整段 travel 的起始地图。
- 镰刀使用既有挥击几何与保护检查；执行前将实际扫及杂草的可能产物一并纳入容量模拟。稀有草帽、已解锁秘密纸条、夏季花种、特殊订单豆子、后期晶球计入相应预估。未知掉落类型不进入自动清障范围。
- `PlayerPickup.cs`：根据原生磁吸半径生成站位；允许通过清障抵达站位。仍依赖原生吸附，不改磁力、不注入拾取。
- 无法拾取时保留位置、物品、原因和条件，返回 `partial/pickup_deferred_conditions_unchanged`，不冒充完整成功。周围障碍、背包容量、工具、体力恢复或掉落位置改变后可重评；不因新命令或换日盲目重试。
- `SemanticWork` 与清理订单保留未拾取状态，但允许独立资源任务继续。清理区域有残留掉落时不能报全清完成。

接缝审查：直接移动、劳动站位、拾取站位与跨图移动复用相同 Walk 执行入口；清障后的每个节点仍用原生碰撞检查。纯规划不写真实库存。选择工具复用合法槽位入口；取消正在挥工具的清障等待原生落点结算。菜单、事件与地图变化不会在子状态中被吞掉。

## 集中验证

### 离线（实测）

- 416 项域检查通过（新增 8 项清障路径、绕行比较、工具/容量/体力及三目标边界检查）。
- 16 项 Python 检查通过；完整隔离构建通过。

### 第一场原生组合检查（实测）

证据：`work/u7-combined-1/`。同一游戏会话先完成路径清障、目标到达、回家，再继续采木/采石及原失败树场景。

- Farm(64,15) → (59,20)：自动以斧头清除挡路树枝 (60,20)，木材原生入包，原移动目标完成。
- 首次资源子测试错误地传 `until=2500`（劳动允许至 2400），收到 `invalid_work_limits`。这是测试参数错误，未改运行校验、未重启；在同一会话纠正后真实获得 4 木材、3 石头。
- 原失败位置 Farm(70,23) 伐木：拾取期间自动以镰刀清除 (72,22) 杂草，继续拾取，木材 5→22，树液 0→6，地面掉落归零；随后独立采石获得 5 石头。
- 树场景导航 21 次查询共 19.67ms（累计，不是单帧），0 次路径重试，原生行动约 16.17 秒。
- 所有逐步快照、落点、背包和后续任务回执保留。未以域检查替代上述原生结果。

最后对跨图状态归属、随机产物与镰刀范围容量补充了保守检查，中间复验 `work/u7-combined-final/` 在标题界面因旧隔离实例占端口启动失败，未进入存档；`work/u7-combined-final-2/` 查出镰刀范围的可能掉落大于单目标预估，原生容量检查正确拒绝。现已增加明确的单目标工具替代：仅在范围掉落装不下或扫及保护物时启用，并重新核对体力。已补齐语义父任务到移动、跨图、拾取子步骤的体力预算传递。最终版本整场组合检查记录于 `work/u7-combined-verified/`，不将前次失败改写为通过。

最终核验 12 项条件全部成立，结论在 `review-result.json`，原始 `checks.json/result.json` 未覆盖。其中三条原测试断言有误：把“预留全部体力时必须拒绝”当成正确行为，忽略可行的零消耗镰刀路线；随后又在第二个移动回执里找已由第一个动作完成的清障。复核原始回执，体力确实 270→270，清障及目标到达真实完成。脚本已修正，未通过修改游戏状态或伪造回执修正结论。

最终树场景 `pickup_verified.remaining=0`，随后独立采石成功；路径查询 20 次共 18.58ms，0 重试。原生回家、再跨图前往 Forest 均成功。新版本也实际触发了 `scythe_collateral_capacity` 单目标替代并核验消耗。条件不变的掉落暂存分支本次未人为制造故障；其长期行为继续从试玩日志核验，不能与上述真实清障场景混称。

## 七天试玩结果：未通过（实测）

实现提交 `0b11afd`。独立目录 `work/u7-trial-7days-1/`，运行 ID `a7a006828aec46b887cbf98ef704285c`，存档 `449722018`。全新 AgentLab、正常时间、deepseek-flash、单玩家、伙伴劳动关闭；完整请求与响应均留存。

春 1 16:00 执行控制层停止，监督器在 16:10 采到停止状态。真实运行约 495 秒，**完成 0 个完整游戏日、0 次过夜、0 重启**。不是七天通过，也没有并入此前成绩。停止后只做只读取证及打开诊断菜单冻结世界；未补操作继续经营。窗口与暂停现场保留。

### 本轮清障和拾取证据

57 份去重后的原生动作回执中，有 5 次 `clearance_route_selected`、5 次核验后的 `native_route_clear`、41 次 `pickup_verified`。两次镰刀范围容量不足时使用经过容量与体力校验的单目标工具替代。没有再出现上一轮旧掉落强制前置导致的 `pickup_unreachable` 连锁。

实例：12:00 从 Farm(53,31) 前往 (51,31)，普通路线需 25 步；算法选择清掉 (52,31) 的普通石头，原生对象由存在变为消失，再完成后续行动。路线选择与真实清除分别留证，不能只用前者证明成功。

这些证据证明已覆盖的农场静态清障能执行，不能证明所有地图的动态路径都正常。未触发的暂存掉落条件解除分支仍需后续原生验证。

### 直接停止原因：社交契约与可观察信息不完整

`ServiceDeclaration.cs:37` 在社交无 `SocialBasis` 时声明期拒绝；`OpportunityRuntime.cs:89` 起的判断只接受尚未认识的介绍任务对象、生日、相关交付、合适礼物或已有关系目标。

16:00 连续三次拒绝：计划中的 Lewis → 直接调用 Lewis → 直接调用 Pierre，均为 `social_basis_required:choose_current_task_birthday_gift_or_relationship_target`。执行层在第三次停止，业务日志第 1642–1671 行保存完整链路。这三次不是游戏执行失败，也不是背包或体力不足。

**静态确认：**工具说明没有讲清这个自主模式入口约束；进度读数给出介绍任务总进度，未把剩余 `whoToGreet` 名单作为明细提供。`SocialBasis` 能读取这一名单，但模型的详细观察缺失。同样，已经认识/已经说过话的无效果请求在声明层先被拒，不能到达原执行器的“已满足”处理。

**推断：**结合未完成介绍任务与上述校验，Lewis/Pierre 没有形成有效新介绍目标；需要直接输出原生剩余名单才能消除模型猜测。不能把这一推断包装为已经读取到完整名单。本轮拒绝回执已有采购等替代项，模型仍重复社交，因此也不能全部归因于“没有任何替代候选”。

后续应集中修复：暴露剩余认识对象与当天交谈状态；将“请求推进的这名对象已经满足”与参数不合法、真实不可达分开；使工具契约、声明校验及执行器共享这一事实。保留三次无进展停止监控，不靠提示词或扩大次数掩盖问题。

### 新增回归：非农场路线误入清障入口（未修复）

13:40、14:00 两次 Town 社交接近动作报 `route_obstacle_conditions_changed`，现场分别为 Town(0,54)→(11,55)、Town(10,62)→(11,62)。两份回执没有选择清障路线的证据，见 `nonfarm-route-evidence.json`。

**静态确认：**`PlayerClearance.cs:62` 的 `BeginClearance` 对所有普通路径再次检查碰撞，却未先限定 Farm 或明确的清障规划；后续 `ClearanceTarget` 又拒绝非农场目标。因此普通路线中任何被复查为阻挡的节点都会误报清障条件变化。

**待验证：**两次具体碰撞是否来自移动 NPC、路径起点或其他动态站位，当前证据不能精确区分。修复入口应只接管已确认的农场清障路径；普通动态障碍继续走正常重规划，并加入镇上移动 NPC 接近的组合检查。这是本轮引入的集成缺陷，不能宣称路径问题全部修复。三次同因停止后未再修改运行代码或强行续跑。

另有三个不同目标的 `work_effect_not_observed`，运行排除目标后继续。一个样本先割掉 (34,36)，后续 (34,37) 已无目标；另外两个没有工具落点证据。需核对范围挥击后的目标快照与普通劳动衔接，不将其未经验证地归因于新清障算法。

### 部分日数据（不能用于 G3 或整日经营结论）

| 项目 | 本轮观测 |
|---|---|
| 现金 | 500→500；0 采购、0 出货 |
| 生产 | 15 株初始防风草完成播种浇水；原生制作并部署 1 个箱子 |
| 劳动 / 运输 | 108.99 / 370.16 游戏分钟，18.17% / 61.69% |
| 等模型 / 受阻 | 47.04 / 73.81 游戏分钟，7.84% / 12.30% |
| 观察区间 | 06:00–16:00，600 游戏分钟；没有入睡或整日结算 |
| 模型 | 18 请求、18 响应；16 次应用，2 次陈旧回复作废 |
| token | 输入 248,060，输出 5,745；输入缓存命中 77,312（31.17%） |
| 关键事实 | 18 请求缺失容量标量 0；库存归属缺失/不一致 0 |
| 重试 | known_failure 空转 0；社交声明同根因 3 次触发停止 |
| 原生失败 | 3 次劳动无效果、2 次镇上路径误入清障 |
| 性能样本 | executor 最大阶段耗时 170.40ms；schedule 142.13ms，均不是总帧耗时 |

44 次顶层工具调用：work.run 9、player.social 6；farm.plan、farm.cleanup、goal.create、plan.submit、menu.close 各 3；player.travel、player.service、shop.read、inventory.read 各 2；领取初始礼物、day.routine、progress.read、day.plan、world.read、farm.select_seeds 各 1。player.buy/procure/ship、百科、任务板及接任务均为 0。

报价里实际允许买种子，模型明确选择保留 500 金、认为已有 15 株足够；本次零采购不能归因于 300 金程序上限。但仅第一天部分数据，不能推定它后续永远不会投资。

### 交接与证据定位

- `result.json`：独立试玩失败与停止原因。
- `native-logs/9177f67d7b1b4cecbd26f7a5dc07050e/day-0.jsonl`：业务事件、原生回执、停止链路；同目录模型文件保留完整调用。
- `clearance-audit.json`：由 `scripts/report_u7_trial.py` 按 run 和 command_id 去重汇总；每条附原日志行号。
- `u6-continuity-audit.json`：容量事实、归属、模型及失败审计，复用已有脚本。
- `paused-diagnostics.json`、`frozen-diagnostics.json`、`paused-progress-read.json`、`paused-inventory-read.json`：暂停及冻结现场。
- `paused-farm-inspect.json` 返回 `unknown_tool`，不是农场证据；作物与箱子结论使用已有原生业务回执，不用这个失败查询证明。
- `report-evidence-index.json`：本次报告引用文件的 SHA-256，不覆盖监督器原索引。原始数据仅在 work 下，不入 Git。

下一轮先修非农场入口回归与社交事实/回执接缝，合并验证农场清障、镇上动态站位、已认识与未认识的介绍对象，再按授权从全新起点重跑。当前保持停止，不将本次局部农场成功当作七天验收。
