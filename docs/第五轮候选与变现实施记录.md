# 第五轮：候选视野与变现闭环实施记录

2026-09-22。依据《第五轮优化方案-候选视野与变现闭环》。范围仍为阶段 A；本轮七天不抵扣十四天，不开展 R-B1 或阶段 B。下文状态随原生证据更新，不能用编译通过代替经营验收。

## 实现与接缝检查

| 项 | 实现 | 保护旁路 / 事实来源 |
| --- | --- | --- |
| U1 | 变现、空地报价、原生任务板、任务交付/领奖、共同目标下一步、具体缺料百科进入统一候选；先保留各类候选再补变体 | 变现复用 BusinessShipment/BusinessSaleStock；报价复用 T4；共同目标复用 AgentGoalPrepare；任务来自原生对象。空地观察只是地形证据，最终布局仍由 farm.plan 核验，不承诺空地数就是可扩种株数 |
| U2 | 删除 17:00 前吞显式出货的调度分支；实际点击按原生库存与出货箱守恒核验 | player.ship 与 player.ship_items 都经过同一调度边界；保留自动合批建议，明确调用立即执行 |
| U3 | 执行失败与模型即时错误都追加当前可行替代项；候选不会产生无依据社交；自主社交声明校验任务、生日、正向礼物或关系目标依据 | 复用原生任务/友情/礼物条件；不把模型自己写的理由当证据；失败根因不被替代项覆盖 |
| U4 | 服务参数检查复用执行器原生 Action 扫描；错地点给正确地点；放置设施缺 item 直接报缺参；farm.plan 检查所在地 | 工具入口、队列声明及原生执行前均检查；柜台站位算法未改 |
| U5 | 22:00 提示；原生 26:00 昏倒前至少留 60 分钟，无有效计划才兜底；远地图沿用 90 分钟返程估计，最早 24:30 接管。劳动默认窗口至 24:00，候选预算与返程预留同步 | 不打断有效计划；原生时间不加速；没有删除兜底 |
| U6 | 农场维护摘要、受阻回执、相关劳动面板共用保护原因：牧草、果树/采集器树、承诺材料 | 不额外改变保护范围；牧草描述说明有筒仓才收干草，避免错误承诺 |
| U7 | 记录田块实际得分与已耕/未耕、每次原生出货点击及数量、柜台扫描/站位/失败类别 | 未修改田块评分、出货分批、柜台站位顺序；运行数据在 work |
| U8 | report_fifth_round.py 从实际请求体分解上下文，并报告 API 的真实 token 与缓存命中 | 字段体积用 JSON 字符数，不冒充 token；未更改压缩预算与规则 |

新加文件为游戏适配层，不是纯域文件；夜间纯规则修改在已显式注册的 SurvivalPolicy / DailyAgenda 内，相关域检查同步更新。没有原生库存、配方、经验或统计的直接赋值。

## 组合检查（原生）

使用 `AgentLab_449679938` 既有原生检查点；测试不保存、不改钱/物品/技能/日期。该检查点只用于组合检查，绝不作为本轮七天起点。

- `work/u5-native-1`：构建锁仍被占用，启动被正常拒绝，无游戏动作。不是业务失败。
- `work/u5-native-2`：Flash 上午实际出货通过；随后脚本直接在开门前购物，被 `shop_closed` 正确拒绝。脚本修为提交调度队列等待原生营业时间。模型测试开始前遗漏退出脚本探针模式，已补暂停→模型接管步骤，不修改游戏事实。
- `work/u5-native-3`：原生出货、错地点服务声明拒绝、设施缺参、等营业时间进店、原生采购、回农场规划并种下两包种子均通过。百科候选实际已由复用模块产生，旧测试只认新增 ID 导致误判；调整为核对真实目标依赖，不为凑测试重复生成候选。
- `work/u5-native-remaining`：补充百科/任务板/面板原生证据，全部通过，见对应 checks/result 文件；实时面板证据为 protection-panel.json。

组合检查共 20 个不同检查点取得通过证据。离线构建与 401 项域检查已通过，Python 测试 15 项通过。原生结果见上述独立证据，不用域检查替代。

## U8：上轮增长来源（已取证）

仅过滤上轮 run `d02c0933028d4a42b3190f3442819bea` 的 106 个真实请求；结果 `work/u5-previous-attribution.json`。

| 部分 | 单次平均 JSON 字符数 |
| --- | ---: |
| memory | 9,832 |
| progression | 7,614 |
| day | 2,754 |
| business | 2,016 |
| recent | 1,989 |
| schedule | 1,714 |
| operating_candidates | 673 |

固定 system 文本每次 8,618 字符；用户上下文各字段体积之和从首轮 12,082 增至末轮 36,263。末轮 memory 里 daily_activity 约 19,975 字符，另有 days 约 2,558；progression 里 nearby_achievements 约 3,124、active_quests 约 1,461。

结论：上轮增长主要来自累计当天记忆和进度摘要，不能归咎于候选数量。后续应讨论事件聚合/增量事实表达；本轮不擅自扩大压缩，先对照新增候选后的真实请求成本与空转变化。

## 七天验收

待组合检查全部具备证据后，从全新 AgentLab 起点、正常速度、Flash 模型运行七个完整原生日。保留完整模型请求/响应、回执、原生存档、每日 A–E、U1–U8 与历史指标。

G3 未自行设阈值；七天完成不等于十四天闸门通过。遇到同一阻断连续三次仍停下报告，不拼接。

## 首次新开局中断与修复

`work/u5-trial-7days` 在春 1 06:10、0 次过夜时暂停。初始礼包原生领取成功；模型随后主动调用了 `knowledge.search`，返回数组。新增 `RecordOpportunityAdoption` 错把观察结果一律当对象读 status/task_id，触发 `decision_receipt_failed_preserve_evidence`。这是本轮日志适配缺陷，不归因于模型。

修复复用已有 `DecisionBarrier.Text`（该函数已有数组、null、标量、非字符串属性的域检查），保留原始查询回执。新增原生 `knowledge.search` → 回执记录 → 决策续接检查。失败运行保留，不计入重新开始的七天。

数组回执修复已通过 `work/u5-array-regression/` 原生复验；任务板与保护面板同步正常。任务板期限的原生字段在接取前为 0，已依据 `work/NativeBillboard.cs:370` 的原生接取规则展示“接取后两天”，接取后直接读取实际 daysLeft，避免把未接任务误报为当天过期。此处是只读规则映射，不写入原生任务。

U7 组合样本：两格新规划全部选中已耕地，候选图中有 21 格已耕空地，实际得分 114.8。此单一样本不代表所有布局。旧七天三个服务失败逐条关联原模型调用：春 2 `Farm + build`、春 4 `Town + FishShop`、春 5 `Town + SeedShop`，均为参数错配（3/3）；这批样本没有证明柜台站位算法失败，因此未改站位算法。
