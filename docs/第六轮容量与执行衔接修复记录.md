# 第六轮：容量、任务衔接与收工复核

依据：用户要求修复上轮七项问题，集中修改后成组原生验证，再从全新起点自主经营七天。正常速度、Flash、单玩家阶段 A；不开发清障寻路或阶段 B，不将七天拼进十四天。

## 改动与证据分级

|问题|落地方式|验证状态|
|---|---|---|
|较大容量请求失败后，小请求也被禁用|容量约束记录操作和请求格数；先检查当前请求是否已经满足。存货达标不要求先发生一次转移|首组原生通过：已有空格满足本次要求时，零转移、零仓库往返；高要求仍受阻|
|混淆随身和仓库|同一库存快照明确输出 carried、warehouse、transferable_to_warehouse；压缩仍保留所有权与容量标量；日记明确为历史|首组强压缩与真实库存观察通过；模型是否继续误读须在七天逐轮复核|
|过早排定睡觉|记录排定时的日期、时间和行动进展；到收工节点若已变化，取消旧睡眠步骤并要求基于最新候选重新决定。模型仍可直接确认睡觉，无新增固定体力下限|首组在原生现场注入旧任务元数据，实际派发前复核生效，未睡觉、未改时间；连续游玩效果待验|
|每种可售物额外留两个|取消统一加 2；保留现有真实任务、生产与补给预留；库存分组只计算一次分配|首组真实单件物品进入候选并完成原生出货|
|首个失败拖住独立工作|after 仍是真实成功依赖；sequence_after 只代表先后顺序。模型计划声明受阻时保留独立任务；一轮内失败后只继续自包含动作和观察，原生局部购买、放置等依赖动作不能绕过|首组存货声明被拒，独立原生回农场成功，真正依赖步骤继续受阻|
|偶发调度慢帧|没有容量约束时不再为每次准入重复搜索仓库路径；外部语义任务占用角色时，先排除再做空间排序；出售复用一次物资分配。将原先混在 schedule 的日常、目标、模型回执阶段分别计时|代码、域检查已完成；不预先宣称消除卡顿，七天分项报告实测耗时|
|坐标为空、第三次失败后又发调用|服务台坐标显式输出 x/y；执行层同时计数声明与终结回执，第三次立即暂停，保留现场；按唯一尝试去重|坐标首组原生通过；首组发现记忆拦截附证据 ID 导致根因分裂，已修正执行层及监督脚本，整组复验已通过|

## 组合验证

- 域检查使用独立输出目录，避免此前遗留进程占用 tests/bin；408 项通过。
- Python 16 项通过，包含首次失败与带 evidence 的记忆拦截共用根因计数。
- 首组 `work/u6-combined-1/`：同一场游戏完成全部十项检查，9 项通过，最后的重复失败停止检查未通过；没有将其掩盖为通过。
- 根因：首次 `unknown_tool` 和后续 `known_failure_conditions_unchanged:unknown_tool:evidence=...` 被分开计数。现在只剥离记忆回执包装，不改变真实错误根因。
- 复验 `work/u6-combined-2/`：使用同一组检查重新验证，10/10 通过；执行日志仅有三次失败调用，预排的第四次 inventory.read 未执行。
- 夹具仅 AgentLab，未添加/销毁物品，未写经验、配方、金钱或统计；缓存与旧任务元数据注入均明确标注，不冒充自然产生的游戏状态。

## 实施纪律检查

1. **保护是否有旁路**：声明与实际派发都经过容量准入；独立步骤仍需自身工具条件。第三次失败在执行层生效，监督脚本仅保留第二道观测。
2. **事实是否有第二来源**：随身/仓库列表来自同次实际读取；历史不代替库存；出售使用同份分配；错误包装与原始根因统一。
3. **全局状态不变量**：本轮不直接写全局玩家身份、选中工具槽、真实库存或原生进度。规划元数据不是实际产出。

## 七天待验项目

独立新起点、正常速度、0 人工补操作，完整保存第八天早晨；记录全部请求/响应、工具回执、原生动作与根因。结束或阻断后保留窗口。

报告包括：每日劳动/运输/模型等待/自然等待/受阻，现金/库存/作物/资产，采购与再投资，候选和工具调用分布，睡眠复核及模型决定，存货实绩与空转，首次/记忆拒绝统一计数，分阶段主线程耗时与模型 token/缓存。G3 不自行设阈值。

## 长程前补充核验

首个新起点 `work/u6-trial-7days-1/` 在春 1 08:40 主动撤回，未完成任何一天。代码复核发现：成功准入跳过昂贵容量刷新后，失败监控也可能沿用未更新版本。现改为仅在失败发生时读取当前容量事实，再记录原始回执和计数；成功动作继续避免无用仓库寻路。没有把该短片段计入七天。

第三场组合验证新增“连续两次失败 → 真实取货改变库存 → 再次失败从新版本计数 → 第三次同条件失败立即停止”；原有各项仍一并执行，13/13 通过。原生证据 work/u6-combined-3/stop-audit.json 中的计数顺序为 1、2、1、2、3，第四个调用未执行。

## 独立七天重跑：未通过，按规则停止

- 运行：`work/u6-trial-7days-2/`，存档 `449719685`，run `dfab4d82502a4ecaa5b27f821ef75fa5`。
- 代码 `f916daf`；真实启动点春 1 06:00、500 金、270 体力。正常速度、Flash。
- **春 1 11:50 第三次 `pickup_unreachable`，执行层停止。0 次完整过夜、0 重启、0 人工补操作。七天没有完成。**
- 一次真实采购 6 包防风草、花费 120 金；累计 21 株完成种植和浇水；停机现金 380、体力 148。没有用花钱或在地作物冒充已实现利润。
- 后续仅做只读取证，未恢复 AI。窗口已保留；人工打开暂停面板冻结世界时为 15:00，该段时间不是试玩成绩，停止时刻仍以执行日志 11:50 为准。

### 三次阻断的证据与根因

|时间|任务|实际结果|
|---|---|---|
|11:40|采木 30|砍 Farm (70,23) 的树，已入包 8 木材，拾取阶段失败|
|11:40|独立采石 20|调度器确实放行，但采石尚未开始就进入附近旧掉落的 pickup_recovery，同样失败|
|11:50|木材库存目标 60|再次进入旧掉落拾取，原生结果失败；同容量版本 3 的第三次失败，立即停机|

**实测：**停止后地面仍有 9 块木材，分成 3 个原生 debris 组，约在 x=73–76、y=24–25；没有销毁或直接写入背包。地图快照、三次任务回执、第一次砍树的原生挥击效果均留存。`pickup-stop-evidence.json` 核验执行层停机后工具调用数为 0。

**静态确认：**`SemanticWork.cs:323–326` 会在采石、采木、采集等任务启动前强制拾取周围 256 像素的掉落；既没有检查该掉落是否已因不可达失败，也没有让独立工作绕过此维护步骤。调度器的独立性修复已生效，但底层这个强制前置重新把任务拦住了。

`PlayerPickup.cs:84–97` 在掉落组中心邻近 3×3 格中寻找满足吸附距离、通行和路径条件的站位，所有候选失败就直接抛 `pickup_unreachable`。本轮该分支没有记录逐候选的排除原因。附近确有密集障碍，**不能仅凭现有证据断言扩大搜索就一定能捡到**。

下一步应先隔离不可达掉落：记录组、位置、失败条件和可恢复事件，保留在地物品；其他任务不强制重试同一拾取。增加候选站位/距离/碰撞/路径排除证据，分清算法搜索不足和实际被障碍隔离。采到的 8 木材保留为部分成果，未捡完不能报完整成功。这里没有开发或提前开放 R-B1 清障寻路。

### A–E 与专项数据

|项|本轮结果|
|---|---|
|A 每日时间分解|没有完整游戏日，无实际入睡时间；不生成完整日占比、不据此定 G3。原始逐帧及状态分类已留存|
|B 重复与早睡|3 次同版本 pickup_unreachable；执行层立即停机；无过夜。睡眠复核长程尚未触发，只有组合验证证据|
|C 经营变化|500→380，采购 6 种子、21 株已种已浇；尚无成熟收获、到账收入或再投资闭环|
|D 模型调用|14 请求、14 响应；输入 172,904，输出 4,172；缓存命中约 36.42%；35 次顶层工具回执，原生子动作另有日志|
|E 整备缺口|本轮未形成三天/七天样本，详细形状计数见 u6-audit.json；不以无触发证明全面支持|

全部 14 份请求均带随身/仓库所有权，和同份原生背包快照比较 **0 缺失、0 不一致**。模型仍曾误述已浇水状态后自行纠正；不能把信息正确提供等同于模型永不误读。

工具分布：work.run 7、player.social 7、progress.read 4、player.service 3；farm.plan/player.travel/inventory.read/world.read/menu.close 各 2；初始礼包、day.routine、shop.read、farm.select_seeds 各 1。百科调用为 0。详细调用和原文在 `u6-audit.json` 与原始 model JSONL 中。

### 卡顿仍未全部消除

同一父任务内动作交接中位数 1.115ms、P95 7.124ms；模型等待中位数 2.634 秒。两者不同，不能把模型等待当作渲染卡顿。

首帧附近 Together update 最高 410.59ms；09:20 仍有一次 354.06ms，分项为投资 231.74ms、调度 118.51ms。相比旧版已经能区分这些阶段，但**尚不能称为卡顿修复完成**。单动作寻路累计最高 201.05ms（41 次查询），不等于一次寻路或单帧耗时。

现存 `partner` 阶段计时包含此前的质量/自动菜单/生存检查，不能把这一段归因于小禾。后续应进一步拆开这段计时，并针对投资与调度的重计算取证优化，不通过加大模型压缩掩盖耗时。

## 交付边界

集中实现已提交，408 项域检查、16 项 Python 检查、最终 13 项组合原生检查通过。七天长程未通过；新拾取前置阻断和剩余慢帧未解决，不能标记整个任务或阶段 A 完成。遵守同一阻断三次停止规则，未继续第四次尝试，未进入阶段 B。
