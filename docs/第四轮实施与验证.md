# 第四轮实施与验证

依据：第四轮优化方案-不变量与选品权。2026-09-21。

## 状态

T1–T7 已实现，组合原生检查通过。三天基线尚未开始；完整基线数据另写交接报告。未进入14天、R-B1或阶段B。

## 改动与接缝检查

|项|实现|旁路与事实来源|
|---|---|---|
|T1|PlayerSelection.Set 集中校验槽位；裸手选择合法中性格；失败收尾断言|Together 的直接赋值仅保留该入口；不增加锄头整备依赖；世界退出后不对空世界断言|
|T2|PreparationCredits 按有序输入/输出消耗预测额度；工具并集；原生已观察商品单位数和配方产量抵扣后继；service整备失败进入部分采购恢复|预测额度只在规划副本，不能重复供应两个后继；每个实际阶段仍读取并消费真实库存|
|T3|ExecutionContract 输出 disposition 与 resume_policy，ReceiptSummary 和 ContextCompression 均保留|不是仅在 OperationsPolicy 日志上改标签；真实回执、计划查询、压缩历史共用同一结果|
|T4|现场报价附生长、售价、劳动、预算与可购买数量；farm.select_seeds 让模型明确选择；投资等待选择再计算布局|farm.execute 也检查选品授权；原生价格/预算校验不变；正在执行的计划不能被再次选品替换|
|T5|清洗技术片段，保留中文句子；从真实运行任务 purpose 显示行动原因；统计意图覆盖|不把模型自由文本冒充任务归属，缺失归属计为未覆盖|
|T6|投资依赖受阻、种子实际到货时保留已批准田块；清理使用与采集一致的种植能量预留；空间/日常浇水/当日体力原因分开|不因投资 blocked 而释放已到货种植预算；上下文包含原失败节点，便于重规划|
|T7|原生购买核验后写入既有 modData 经营账本；Facts、界面继续从同一账本读取|旧 Purchased 仍按购物订单 ID，新增 PurchasedItems 按商品统计；回执ID+索引去重；Squad序列化保留新增字段|

全局状态写入增加第三条纪律：任何赋值必须先满足对应不变量。保留原有两条：保护是否有旁路、同一事实是否存在冲突来源。

## 原生组合证据

- `work/round4-combined-3/`：同一AgentLab进程，原生建仓和照料，Flash根据现场报价选择花椰菜与甘蓝各1，支出150；两种采购→播种→浇水完成。SpentToday=150，与原生现金500→350一致。
- 同进程原生整备卸走锄头→镇上采集；负数选中槽位在赋值前被拒绝；空目标原生动作返回not_a_forage_target，后续绘制/任务继续；范围清空回执already_satisfied。
- `work/round4-edges-1/`：三种原生回执处置各有实际结果；service恢复使用明确的调度故障注入（不是声称自然发生），保留ReservedToday=100，库存前后完全相同。
- `work/round4-combined-1/`与`-2/`未加载存档：上一轮SMAPI崩溃标记阻止启动。旧日志与marker已移入`-1/`保留，第三次启动成功，不冒充三次游戏业务试验。
- `work/round4-domain-final.log`：离线域检查；不替代原生证据。默认测试输出目录遇到文件锁后，使用独立work/Round4DomainFinal输出，未关闭其他进程。

主要组合之后补充的改动是：退出世界时断言保护、受阻种植需种子实际到货、清理与采集统一预留、service恢复夹具；补充组合使用更新DLL。随后追加选品只接受等待阶段、上下文展示失败节点。各运行manifest保存自己的DLL哈希，不宣称不同构建相同。

## 面板设置

隔离Mod的 `Together/config.json`：`AgentOverlayOpacity` 范围 **0.2–0.9**，越小越透明；修改后重启加载。滚轮滚动面板、点击标题收起；原始JSON、错误码和ID留在日志。

## 限制与待验证

- Flash选品基于真实依据，不保证比算法排序更优；本轮夹具明确要求两个不同品种各1包，属于能力测试，不是自由经营基线。
- 行动意图覆盖按动作/意图变更采样，不能称逐帧覆盖；直接实验工具等没有调度任务归属时如实记为未覆盖。
- 第一个完整组合覆盖主要原生链；新版边界组合不重复旧的满包制作与289组堆叠检查。
- 三天自主经营是否减少重复“已满足”操作、跨日劳动是否合理，必须等独立基线结果。G3仍由用户确定。
