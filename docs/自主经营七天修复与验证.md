# 自主经营七天修复与验证

本轮依据：用户要求修复五天试玩复盘问题、接入任务成就信息，并重新自主运行七个完整游戏日。单玩家、正常速度、DeepSeek Flash，仍属阶段 A；不进入阶段 B，不开发破障寻路，不替代十四天闸门。

## 实现清单（勾选表示代码完成，原生验证另列）

- [x] 日常请求始终携带当前任务、邮件、解锁、原生成就计数摘要；低上下文预算也不退化为查询指针。变化记日志，更多目标可分页查询。不把条件满足当作 Steam 解锁。
- [x] 停止启动时自动建立仓储项目，改为提供带真实条件的仓储候选。AI 创建目标后继续复用原生配方依赖链；真实放置的箱子登记为共享仓储。
- [x] 木材目标同时比较可砍整树与树枝，按真实可达路程、预计体力、挥击和基础产量排序；实际完成量仍以入包为准。保护区、果树、挂树液器的树保留。
- [x] 清理使用镰刀；站位与完整挥击范围检查保护对象，不再因附近任何杂物改用斧头。田块/道路/生产区牧草可割，牧草保留区不清。原生挥击几何只读计算，不调用随机掉落函数。
- [x] 移除残余 17 点体力门槛；每次工具挥击核对原生单击预计消耗和本任务模型保留量。预算正常耗尽返回 partial，保留真实成果，不冒充全目标完成。
- [x] partial 释放角色但不能满足后继物资依赖，不作为无进展执行失败。真正失败的停止监控保持。
- [x] 普通清理单跨天保留成果与未捡位置，暂停等待模型重排。明确设定的重复例行保留其含义。
- [x] 工具能力统一读取随身与共享仓储；钓鱼候选、钓鱼地点和详情共同使用这个来源，执行前沿用真实取料整备。
- [x] 路径搜索、地图格子查询采用同一个角色碰撞判定；派发完整路径、行进前复查下一格，实际移动仍由原生控制器完成，不改位置、不拆障碍。
- [x] 报价直接附可买数量、成熟日、首收基础净值、单位生长期回款和照料估计。预算与保留金仍由模型决定，不恢复每日固定额度。
- [x] 同一模型决策里“选品自动采购链”与显式同商品采购合并，避免两个控制入口重复购买。新决策的额外采购不受此去重影响。
- [x] 无输入菜单的原生过场期间不重新请求模型；显示/记录原生状态，等待动画结束，不跳过剧情。

## 验证纪律

每处变更检查：保护是否存在旁路；事实是否有第二来源；全局玩家状态写入是否维持不变量。
新增游戏几何适配器依赖 XNA，纯路径/排序算法加入已显式注册的 AutonomyPolicy；域检查覆盖路径阻挡、部分完成依赖、清理跨日和摘要压缩。

- 域检查：396 条通过，证据 `work/autonomy-seven-domain-v2.log`。
- 首次组合检查发现第二个背包依赖：FishingLocations 仍只查看随身鱼竿。已改为与候选共享 AvailableTools，未用新增提示词掩盖。
- 原生组合：`work/autonomy-seven-native-4/` 验证真实取鱼竿→跨图→钓获、返回、镰刀、采木预算部分完成与混合清理；`work/autonomy-seven-native-night/` 验证原生过夜、次日订单暂停。
- 检查脚本有三处修正并保留失败记录：读取已结束任务应查完整回执而非活动列表；等待加载淡入结束；睡眠调用补齐必需原因。未把这些脚本失败伪报成游戏成功。
- 真实割除指定田块牧草、树枝/整树的成本选择、选品去重、仓库初始选址与持续经营收益，继续由长程原生日志核验；不以静态实现代替效果。
- 七天自主试玩从全新起点运行，证据全部放 `work/`，不提交原始请求、存档或密钥。

## 必须如实保留的边界

材料产量、天气和额外掉落是估计，不能提前记入库存。任务/成就摘要是信息而非强制优先级；算法不要求 AI 花光钱或耗尽体力。主线程耗时需要以本轮实际采样汇报；尚未宣称“无卡顿”。G3 仍由用户定阈值。
