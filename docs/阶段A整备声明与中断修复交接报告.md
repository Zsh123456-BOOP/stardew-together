# 阶段 A：R-A5 / R-A6 与组合检查交接

更新：2026-09-21。依据《阶段A阻断修复方案-容量模型与清障寻路》第 11 节及本轮任务。原生证据在 `work/`，不进入 Git。

## 1. 范围与当前结论

- R-A5：声明期拒绝不支持的整备形状；没有实现多箱取料、建设整备或复杂交付。
- R-A6：转移异常先核对真实双方库存守恒；成立则放弃该任务、重读实际库存，不回滚已经接受的原生转移；不成立或不能核验则停机。
- **阶段 A 仍未通过 14 天闸门。没有开始 R-B1 或阶段 B。**
- 三天基线：正在从独立原生新档执行。以下原生组合证据不能代替三天数据或 14 天验收。G3 阈值未设置。

## 2. 实现与证据强度

| 内容 | 实现 | 证据 |
| --- | --- | --- |
| 静态形状拒绝 | `LoadoutSafety.Unsupported`；整备计划及其显式依赖先判定，再看菜单/容量/路线 | 建造与订单交付**实测**：`preparation_shape` 返回 `loadout_shape_unsupported`，前后背包一致 |
| 多箱形状 | 背包不足、没有任何单箱足够、但所有共享箱合计足够，则 `multiple_storage`；不搬第一件 | **静态确认＋域检查**；尚无两个真实箱子的原生触发样本，不称为原生通过 |
| 根因约束 | 复用容量版本账本，增加形状类根因；与物理容量分开，不能阻挡无关劳动 | 静态确认＋域检查；声明拒绝后原生其他劳动可继续 |
| 腾格候选入口 | 不支持的交付候选同样排除，不能绕过整备声明从腾格流程执行 | 静态确认 |
| 中断核对 | 锁定原箱对象身份，重新读玩家＋箱子；按 ID、品质汇总数量；记录完整槽位快照 | 三类异常**实测**：均在第一次原生存取完成后注入，守恒通过、后续采石成功 |
| 核对失败 | 明确守恒错误、总量不等或无法读到原容器，统一 `loadout_conservation_failed` 停机 | 静态确认＋域检查；未破坏真实物品来制造负例 |
| 近箱过度整备 | 删除“离箱子近就卸货”；已带齐用品、预计产物可装下则继续任务 | **实测**：一单目标 50 木材实际收得 54，完成 4 个目标，中途存货 0 次 |

声明范围当前是保守边界，详细映射以 `LoadoutSafety.cs` 为准。不把“业务不做，调度仍可继续”说成该业务已实现。

新增纯算法文件 `LoadoutSafety.cs` 已显式注册到 `Together.DomainChecks.csproj`，检查挂在既有 `CapacityChecks` 中。原生执行不改经验、配方、金币或进度。没有改上游源码或加入清障寻路。

## 3. 原生组合检查

| 项目 | 结果、证据与限制 |
| --- | --- |
| 双容器接近满格 | **实测通过**。`work/ra56-packing-2`：用原生 `InventoryMenu.rightClick/leftClick` 拆分已有物品，背包 12/12、箱子 34/36；整备按可行顺序存取、水壶回到背包；转移前后完整快照见证据索引。空槽填充只展开箱子既有 36 格，不增加容量或物品。 |
| 可恢复中断 | **实测通过（故障注入）**。`work/ra56-faults-2`：`loadout_stock_changed`、`loadout_storage_busy`、`loadout_native_capacity_changed` 分别在第一次真实转移后触发；三次均守恒成立，随后各有一次原生采石成功。不是声称自然发生过三种竞争。 |
| 暂停 / 取消 / 续接 | **实测通过**。`work/ra56-pause-1`：赴仓整备时暂停，子动作停止，旧任务需重审；取消后重新读取真实库存并提交，取回水壶。没有自动重放旧转移序列。 |
| 清理 → 播种 | **实测通过**。`work/ra56-cleanup-1`：新档原生礼包；清理单上限 3；15 格播种依赖清理；清理没有被尚不可执行的播种任务饿死，之后实际播种浇水。预算与布局在 `committed-care-budget.json`、`plant-layout.json`；没有添种子、恢复体力或改时间。 |
| `pickup_unreachable` 的经营影响 | **实测通过（调度故障注入）**。`work/ra56-remaining-combined-1`：在观察到真实掉落物后注入该错误；相同任务在提交期就被 `known_failure_conditions_unchanged:target_abandoned_today` 拒绝；改派采石成功，未进入降级模式。此前 `work/preparation-native-1/native-wood.json` 是自然拾取受阻证据。本轮自然采木获得 54 木材，未再次自然复现该路径，因此不宣称拾取或寻路本身已修好。 |
| 第三次同根因停止 | **真实日志重放通过**。`work/ra56-root-watch-replay.json`：从此前真实失败日志按事件顺序取得原生回执和容量版本 10，监督器在第 3 个不同命令失败时返回停止；重复读取同一条不计数。不是新游戏中人为再制造一次死锁，也不是 14 天无人值守证据。 |
| 动作 / 模型 / 寻路 / 帧耗时 | 指标已分开；Flash 两次真实响应及当前帧耗时见 `work/ra56-remaining-combined-1/performance.json`，细项下一节。 |

证据检索：`work/ra56-native-evidence-index.json` 包含逐步 JSON 文件及 JSONPath，指向运行内 Journal 的完整 before/after 快照。早期整备夹具重建 Autoplay 状态后没有重新挂持久日志监听，事件由每一步 HTTP 原生快照保存；正式基线通过 StartAutoplay 挂载日志，不存在该夹具日志缺口。既有满包造箱、289 组堆叠比较、同容量存货拦截和工具取回种植的通过结论未重跑；本次造箱只作为获得真实仓库的前置，不重新计作新验收成果。

### 测试组织与失败记录

最初按脚本模式分开启动游戏，造成了用户指出的反复重启。剩余拾取恢复与 Flash/性能检查已合并到一次运行；以后同一运行尽量成组检查，只有换 DLL 或独立基线起点才重新加载。

- `ra56-packing-1`：夹具向未展开槽位放置失败，未进入待测整备流程，退出且不保存。夹具现已在原容量内补空槽，并为拒绝放置增加原生还原与失败守恒停机。
- `ra56-faults-1`：第二个测试任务没有实际转移，故障注入未触发。调整测试任务以确保真实转移，不修改业务结果。
- `ra56-cleanup-1` 的脚本总结果为失败：最后一项要求自然复现拾取错误，但实际采木成功。前两项清理与播种有独立原生成功证据，没有把整份脚本改成成功。
- `ra56-pickup-injection-1`：重复工作在提交期已被正确拒绝，脚本原先只在完成回执中等待该错误，提前退出。修正脚本接受真实的提交期拒绝，合并运行后完成后续采石验证。

没有将这些失败运行拼接为连续经营天数。

## 4. 性能与时间口径

### 组合测试已取得的数据

- Flash 两次响应：输入 7,684 / 8,129 token，输出 121 / 170，缓存命中 1,280 / 2,560；等待 1,914.6 / 1,410.0 ms。
- 本轮 Together 主线程 Update：325 个样本，平均 2.52 ms、p95 0.246 ms、p99 10.42 ms、最大 **387.49 ms**。
- 决策快照阶段最大 105.51 ms、上下文阶段最大 68.32 ms；这些是分段测量，不能把分段最大值相加当作同一帧完整归因。
- 本轮 investment 没有开启投资业务，其最大 0.057 ms，**不能据此宣布原 394 ms 投资卡顿已解决**。
- 动作衔接来自 `work_handoff.delay_ms`；寻路来自每个原生动作的 `navigation_summary`（成功和失败均记录）；三天基线报告按四类分别给分布。
- 帧指标只覆盖 Together Update，不是整个游戏渲染帧。日内均值/最大覆盖全日；分位数保留最近至多 36,000 次 Update，范围在 JSON 中明确标注。

**思考时卡顿尚未消除。** 本轮按任务要求测量、区分原因，没有扩展为主线程规划器重构。

### 三天 A 项的新口径

原 `EffectiveLaborMinutes / 1200` 字段保留为旧口径，不能混作本轮 G3。

新增：实际原生上床确认时刻、06:00 至该时刻的可用分钟数；劳动、运输、等待模型、等待自然生长、受阻五类分配。每个原生游戏钟间隔按所观察的状态真实秒数加权；未观测的起始区间保守计受阻。另记真实秒数，避免菜单冻结游戏钟时把模型等待漏掉。

正常工具动画、移动、原生剧情/菜单、等待模型、动作等待无位移、无动作空闲另有明细。原生菜单/剧情没有观察到劳动时先计“受阻”，明细保留其原因，不把这种正常等待误称成无意义发呆。G3 阈值仍由用户根据基线定。

## 5. 三天基线 A–E（待本轮运行结束填入）

运行目录：`work/ra56-baseline-3days-1`；正常时间、Flash、新建独立 AgentLab，无人工补操作。监督器保留日志事件在发生时的容量版本，避免轮询时用新版本误标旧失败。

- A：每日五类分钟/百分比、实际入睡时刻、真实秒数与明细。
- B：存货任务与整备次数、失败根因和重复次数、早睡时刻及原因。
- C：每日初 / 入睡前原生账本，以及次日结算后的现金和资产变化；未成熟作物单列，不估作已兑现收入。
- D：响应数、输入/输出 token、缓存命中、模型异常与转移恢复次数。
- E：不支持整备形状的拒绝次数，按形状分组，并单列同版本再次拒绝。

数据由 `scripts/report_survival_baseline.py` 按唯一 run ID 读取原生日志生成；不改写原日志。

## 6. 未实现与下一道闸门

多箱整备、建设备料、复杂/部分交付的完整执行，以及伙伴 BotFarmer 库存迁移仍留到阶段 B。清障寻路留到 R-B1。正常劳动中的未知掉落仍按真实容量处理，不做默认丢弃、出售或背包升级。

之后顺序保持：用户确认 G3 → 独立 14 天、0 次重启验收 → R-B1 → 阶段 B。三天不能计入 14 天。
