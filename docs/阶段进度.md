# 阶段进度

## 陪伴阶段 B：DeepSeek 与人设、自主行为（2026-09-18）

- `deepseek-flash` 已实际接入，Key 仅存项目 `.env`；配置样例不含密钥。
- 接受、拒绝、玩家显式强制、检测真实空闲后自主挖矿四个场景游戏实测通过。
- 最终完整验收轮四次模型调用、7630 tokens，模型响应 1.069–1.506 秒；不包括之前调试调用，非整体游戏动作耗时。
- 十二项单元检查通过；模型无效结构最多修正一次，失败不派工，推理后重验地图和目标。
- 人设文件可编辑；原生好感与近期交互结果已进入上下文；有限自主观察、冷却、相同环境去重已实现。
- 当前为命令行入口，未接游戏内聊天 UI、独立钓鱼及长期承诺恢复。完整说明见 `陪伴阶段B验收与运行.md`。

## 陪伴阶段 A：指定 NPC 工具闭环（2026-09-18）

- Squad 适配层、通用桥接、隔离构建和启动完成。
- 指定角色真实挖矿、结果归因、去重、冲突拒绝、旧会话拒绝、失效目标拒绝、跟随模式和取消八项游戏实测通过。
- 详细范围见 `陪伴阶段A验收.md`，模型行为单独在阶段 B 验收。

## 陪伴方向：三仓库准备与衔接审查（2026-09-18）

- 已将 Squad、StardewValley-MCP、AI Dialogue 三个 Git 仓库克隆到 `external/`，固定 SHA 见 `configs/companion-upstreams.json`。
- 按用户要求删除 Squad 的完整测试目录（25 个 C# 文件，9072 非空行），并清理解决方案引用；本项目已有验证保留。
- 新增可复现准备脚本；第三方源码保留独立 Git 历史，不重复提交到主仓库。
- 明确新定位：C# 负责 NPC 行为与状态，Python 负责人格／关系决策；Squad 作为唯一行动执行者，聊天和工具接口统一接入。
- 源码确认的主要缺口：指定 NPC 派工、控制权与回执；Squad 钓鱼收获依赖玩家钓鱼事件，远离玩家会清任务追赶，不能直接当独立跨地图劳动底座。
- 完整衔接、验证顺序和构建注意点见 `三仓库衔接方案.md`。本阶段是源码准备与设计，尚未实现或游戏内验证三仓库集成。
- 已验证准备脚本幂等、删除范围、三个固定版本、解决方案解析及现存项目 XML；Squad 运行源码无改动，其他两个克隆保持干净。

## 环境与仓库准备

- macOS arm64；Steam 游戏为 x86_64。
- 已安装星露谷 1.6.15、SMAPI 4.5.2。
- 找到可用的 .NET SDK 6.0.428（arm64），复用已有工具，无需重新安装。
- 原游戏 Mods 中有 FlyDojo；本项目使用独立 Mods 目录。
- 已初始化 Git，并固定 Farmtronics 上游 `a59fc65bdb263d257d0ecd453202b65c6269f7a5`。

环境准备已完成；阶段 0 的游戏内验收结果如下。

## 阶段 0：底座接通（2026-09-18）

- 固定上游在本机编译通过；新增 API 在临时构建副本中编译，上游子模块源码保持干净。
- Farmtronics + AgentBridge 在星露谷 1.6.15 / SMAPI 4.5.2 实际加载成功。
- 创建独立 AgentLab 角色；原有个人存档和 FlyDojo Mod 未改动。
- 通过真实工具使用验证浇水：目标地块从干燥变为已浇水，水壶 40 → 39。
- 通过原生收获与机器人拾取验证产物：防风草进入 bot-1 背包，而非仅根据作物消失判定成功。
- 测试使用显式初始化的小地块与物资，并冻结游戏时钟；正常执行期间未通过修改坐标或库存伪造完成。
- 本地 HTTP 仅监听 127.0.0.1，使用本地令牌；主线程队列访问游戏，含会话校验与命令去重。
- 可复现实测入口：`python3 evals/stage0_smoke.py`。详细动作证据保存在忽略提交的 `outputs/stage0-evidence.json`。
- 上述脚本已通过六项检查：真实移动、浇水效果、收获入包、安全边界取消、重复命令去重、旧会话拒绝。
- Mod 无运行异常；直接开发启动时 Steam 未启动，游戏提示成就不可用，不影响本地控制验证。

阶段 0 时的限制：只控制玩家当前所在地图；仅支持普通手摘作物，镰刀作物尚未开放；暂停在单格动作结束后生效。高层任务调度在下一阶段补齐。

## 阶段 1：完整技能闭环与反馈恢复（2026-09-18）

- 实现 A*、合法交互站位选择、整片浇水/收获、返回任务。
- SQLite 持久化任务和动作证据；支持暂停请求、动作边界确认、返回后恢复；进程间角色锁避免重复控制。
- 用户选择暂不配置模型接口，由 Codex 阅读状态、生成计划并调用工具。
- 三轮完整任务实测通过；每轮 12 次浇水、6 次收获，背包新增 6 个防风草，最后返回起点。
- 暂停/返回/恢复实测通过；已完成目标没有重复浇水。
- 第二台机器人真实走入原路线后，第一台发现道路阻塞并重规划绕行，实测通过。
- 不可达目标被明确标记失败，机器人没有移动到目标，也没有错误宣称完成。
- 详细数值与范围见 `阶段1验收报告.md`。

后续未实现：独立模型服务/聊天 UI、双机器人统一调度、箱子归仓、跨地图任务、MCP 封装。测试中的第二台机器人用于制造真实动态障碍，不等同于已经完成多 Agent 协作产品。
